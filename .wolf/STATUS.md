# STATUS — TDD 对比 + 三目标实现

> Single source of truth for resuming work. Read this FIRST when starting a session.
> Last updated: 2026-09-01

---

## ✅ Done

- **四仓库 TDD 对比分析** → `doc/reference_harness/comparison.md`：结论是「四个仓库谈的是三个不同问题 A/B/C」，共同点只有「只 mock LLM 边界，其余跑真货」。
- **三个 plan doc（各含自我批判）** → `doc/03_tdd_testing/plan.md`、`doc/04_faux_provider/plan.md`、`doc/05_verify_world/plan.md`。
- **目标 1（red-green + 覆盖率闸门）**：`pyproject.toml` 加 `pytest-cov` + `[tool.coverage]`（`fail_under=92`、`precision=2`）；`scripts/check.sh` 单命令闸门；`AGENTS.md` 追加「测试纪律」三规则；`tests/test_coverage_gate.py` meta-test + watch-red 证明。
- **目标 2（faux provider）**：`faux_provider/`（`FauxProvider` + `FauxMaker`），13 测试，确定性 diff=0，~32.7ms/loop。
- **目标 3（verify the world）**：`goal_loop/world_verifier.py`（`WorldVerifier`）+ `loop_runner.py` 最小接线（可选参数 `world_verifier`，fail-closed），9 测试，误完成率 0/5。
- **全系统 E2E 测试** → `tests/test_system_e2e.py`：一次真实 run 串起 8 层（faux + persistence + loop + sandbox + trace + hippocampus + world_verifier + checker），happy path（complete）+ 对抗 path（假 exit-0 + 说谎 checker + 错产物 → blocked）两场景，2 passed。
- **红蓝对抗** → `tests/test_red_team.py`：红队 4 攻击（崩溃 maker/checker、注入变体 `Ignore ALL Previous Instructions`、`deploy` 风险降级）先红；蓝队修复（`loop_runner` 捕获 maker/checker 异常 fail-closed、`safety` 加 `_HIGH_RISK_ACTIONS` 内置 + 注入变体 marker）转绿。4 passed，漏洞记 `.wolf/buglog.json`。
- **全量验证**：`python3 -m pytest -q` → **116 passed**；覆盖率 **92.57% ≥ 92**。
- **收口**：`faux_provider/provider.py:121-124` 判为**行为**（`stream()` 分块语义）→ 补确定性测试（monkeypatch `time.sleep`），`faux_provider/provider.py` 覆盖率 100%；README 补 `faux_provider/`、`world_verifier.py`、`scripts/check.sh`+测试纪律 + 测试总数 79→126；JOURNEY/`doc/04_faux_provider/journey.md` 风险条目勾掉。全量 **126 passed**、覆盖率 **93.11% ≥ 92**。
- **harness skills 调研 → 自我改进闭环（目标 7）**：`doc/reference_harness/harness_skills.md` 把 `AGENTS_REPO.md` 的 100+ 仓库蒸馏成「五台阶 + 三条横切规律 + 三个新目标」；实现目标 1（§5 自我改进）`goal_loop/self_improver.py`（`SelfImprover`：run 结果→`repeat`/`avoid` 教训→下次注入 steering）+ `Hippocampus.facts()` + `GoalLoopRunner` 可选 `self_improver` 参数 fail-open。13 新测试，全量 **126→139 passed**、覆盖率 **93.33%**，`self_improver.py` 100%。
- **src 布局迁移 + 100 任务路线图 + 目标 2/3（无人值守调度 + 多代理编排）**：`doc/roadmap/100_tasks.md`（100 条 P0/P1/P2 + 自我评审）；`goal_loop/scheduler.py`（`Scheduler`：`resume_all`→串行 `run_until_terminal`→晨报，skip/errored 隔离 + `run_periodic` 注入 sleep）+ `goal_loop/orchestrator.py`（`Orchestrator`：planner→executor×N→reviewer，`make`/`check` 接 loop）。全量 **140→154 passed**、覆盖率 **93.79%**，两新模块均 100%。crashed-maker 兜底核实为早已 fail-closed（`loop_runner.py:210-219`），无需新代码。
- **P1 加固（Era 33）**：`eval_harness/judge.py`（`LLMJudge` fail-closed，超时/异常/垃圾回复判 FAIL）；`cost_control/cost.py`（`Price`/`PRICING`/`estimate_cost` 按模型计价，未知模型 KeyError）+ `cost_control/ledger.py`（`TokenLedger` 持久化 input/output/calls）；`observability/trace.py`（`span` 记 `duration_ms`）；对抗补 `resume_all` 幂等 + 多 goal 状态隔离；`sandbox/path_policy.py`（`PathPolicy` 文件写白名单，`..` 穿越 fail-closed，seccomp/network 属 OS-gated）；CI `check.sh` 扩 fmt+lint+test+coverage 四闸 + `pyproject.toml` 加 ruff（line-length 100、E4/E7/E9/F）清空 34 条 lint；`doc/08_scheduler`/`doc/09_orchestrator` plan+journey 补齐；覆盖率补到 **97.18%**（目标 95%）。全量 **154→206 passed**。
- **真 LLM 深度评测套件（Era 35）**：`eval_llm/`（`client.py` 双格式适配 Anthropic/OpenAI + `report.py` 报告收集）+ `tests/test_real_llm.py`（四维 × 5 模型，`RUN_REAL_LLM=1` opt-in，单模型挂掉 graceful skip 不拖垮整批）。5 可用模型：deepseek-v4-pro/flash、grok-4.6、minimax-m3（Anthropic 格式）+ kimi-k2-turbo-preview（OpenAI 格式）；glm code 1113 无额度排除。56 条全绿（5×四维 + 探测 + 报告写盘）。抓出浅基线没暴露的真问题：thinking 模型（kimi）`reasoning_content` 消耗 `max_tokens`，小 cap 返回空 `content` → llmjudge/summarizer 提到 512。真数字见 `doc/10_real_llm_eval/report.md`（含每模型 metrics latency mean±std）。成本指标标待确认（PRICING 无真实模型 ID）。离线新增 11 测试，全量 **206→217 passed**（另 56 opt-in skipped）。
- **25 条 P0 增强全执行（本 quest）**：A 沙箱加固（PathPolicy 接线/环境 scrub/cwd/argv 绕过/HITL 绑定）、B 逐准则判定+证据+人工介入、C 目标循环韧性（in-flight 标记+崩溃恢复、重试退避、RunHistory 账本、QUARANTINED 毒目标、turn 中预算投影、重入 resume）、D 压缩（预算约束折叠、锚点保护、常驻 Do-Not-Repeat）、E 评测/成本/观测（RegressionGate 黄金对照、guard_budget、trace_cost 事件、trace 脱敏）、F 打包（src 布局 build-system、`ah` console script + `python -m agent_harness`、import-smoke/打包元数据/demo 三 meta-test、按包覆盖率闸门 `scripts/coverage_gate.py` 接入 check.sh）。全量 **217→272 passed**（另 56 opt-in skipped）、覆盖率 **95.93% ≥ 92**、按包闸门全 ≥70%、`ruff check` 清空 lint（新增 `agent_harness` 包 + `py.typed`）。
- **品味评分（taste_score，本 quest）**——多 agent 过夜竞争打分，黄金标准 `agent = LLM 头脑(C) + harness 手脚拓边(E) + 安全边界(S)`，成对帕累托判定不叠加记分，**E up & S down = 拉黑不是加分**。五道防 Goodhart 锁：成对比较 / 菜单变异 / 留出黄金组终审 / 回归否决 / judge-executor 分离+轮数预算上限，另加**自述不可信**（`did_expand`/`safe` 经 `verify` 外部证据推出，堵说谎型刷分）。探针从 `doc/roadmap/enhancements_100.md` + `tests/test_red_team.py` 合成；跑法 `PYTHONPATH=src python3 -m taste_score compete`。设计见 `doc/superpowers/specs/2026-09-02-taste-score-design.md`；已写入 CLAUDE.md/AGENTS.md。全量 **272→283 passed**（另 56 opt-in skipped）、覆盖率 **95.49% ≥ 92**、按包闸门全 ≥70%（taste_score 89.4%）、`ruff check` clean。
- **CSDD 集成（方案 B，本 quest）**——把安全边界写死成可版本化+可哈希校验的**宪法**（`src/taste_score/constitution.toml`）：每条 `Principle` CWE 映射 + RFC2119 级别（MUST/SHOULD/MAY）+ `anchor`(真实 `src/` 文件)+`pattern`，`digest()` 哈希、meta-test `test_every_anchor_resolves_and_matches_pattern` 钉死禁止编造安全域。五块：①宪法 → S 显式边界；②`build_initial_probes(constitution=...)` spec 驱动探针（MUST→hold/SHOULD→expand）；③`trace.py` `TraceabilityVerifier` 静态可追溯（证据从 `src/` 读 `did_expand`/`safe`，非自报，L7 自动 100% vs 手工 94%）；④第五道→**第六道锁「禁改尺子」**（`TasteGate.pinned_digest`，宪法被改=整轮判负「ruler tampered」，L4 抗投毒）；⑤`amendments.py` 持续改进（被否决越界行提收紧证据，`ratify` 回归否决+MUST 级需人工，**改进者≠被评分者**）。design/spec → `doc/superpowers/specs/2026-09-02-csdd-integration-design.md`；实施计划 → `docs/superpowers/plans/2026-09-02-csdd-integration.md`（8 任务，T1–T7 已落 + T8 docs）。带宪法跑法 `PYTHONPATH=src python3 -m taste_score compete --constitution src/taste_score/constitution.toml`（默认不带走 demo，五道锁原样）；已写入 CLAUDE.md/AGENTS.md。全量 **283→293 passed**（另 56 opt-in skipped）、`ruff check` clean。
- **[2026-09-03] 宪法扩到 7 原则（本 fire）**：加 SEC-07（CWE-78 验证命令安全执行形式→`src/goal_loop/verifier.py`，锚 `class CommandVerifier|shell=False`，禁 `shell=True|os.system`）——与 SEC-04 的白名单判定互补的**独立一层**：白名单没变，验证命令仍可能退化成 `shell=True` 重新引入命令注入（CWE-78），SEC-04 的锚文件（command_policy.py）不变所以看不见。TDD 先红（`test_command_verifier_safe_shell_boundary_is_constitutional` 无 SEC-07 时 fail）后绿（303 passed / 56 skipped，覆盖 **95.40% ≥ 92**、ruff lint clean、per-pkg 全 ≥70%）。traceability 锁 **7** 原则全 expanded+safe；csdd 仍 **1.0**（饱和，本轮不追分，只加真实覆盖面）。本地 commit `8510788→[新]`（只含 constitution + 测试 + STATUS；前几轮 verifier 回退修复的 gate/trace/__main__/test_taste_score 改动仍留工作区未动）。
- **[2026-09-03] 宪法扩到 8 原则（本 fire）**：加 SEC-08（CWE-703 沙箱**执行层默认拒绝**→`src/sandbox/sandbox.py`，锚 `class Sandbox|def run`，禁 `shell=True|os.system`）——SEC-04 钉的是白名单**判定**（`CommandPolicy` 纯 helper，sandbox.py 并未 import 它），SEC-06 钉的只是环境**脱敏**，两处都没钉住**真正的 subprocess 启动点** `Sandbox.run`：fail-closed default-deny（未配置 allowlist 时 `SANDBOX_UNAVAILABLE` 拒绝而非裸跑）+ 运行时空白名单强制 + shell=False。把这格钉住，否则 default-permit / shell=True 的安全回归不会被任何原则登记。TDD 先红（`test_sandbox_fail_closed_execution_boundary_is_constitutional` 无 SEC-08 时 `assert None is not None` fail）后绿（304 passed / 56 skipped，覆盖 **95.40% ≥ 92**、ruff lint clean、per-pkg 全 ≥70%、anchor meta-test 过）。traceability 锁 **8** 原则全 expanded+safe；csdd 仍 **1.0**（饱和）。本地 commit 只含 constitution + 测试 + STATUS；gate/trace/__main__/test_taste_score 与 security/、command_policy.py 等前几轮工作区改动仍未动。
- **[2026-09-03] 宪法扩到 9 原则（本 fire）**：加 SEC-09（CWE-703 评估层失败关闭→`src/eval_harness/judge.py`，锚 `class LLMJudge|Verdict.FAIL`，禁 `except[^\n]*Verdict\.PASS`）——SEC-08 钉的是**执行层**（Sandbox 无 allowlist 即拒），SEC-09 钉的是同 CWE-703 的**独立层——评估边界**：LLM judge 超时/异常/不可解析回复必须 FAIL 不能 PASS，否则坏结果会穿过闸门。现有原则都锚 sandbox/safety/verifier/injection，看不到 judge.py 被改坏成 fail-open 的回归。TDD 先红（`test_llm_judge_fail_closed_boundary_is_constitutional` 无 SEC-09 时 `assert None is not None` fail）后绿（**305 passed / 56 skipped**，覆盖 **95.40% ≥ 92**、ruff lint clean、per-pkg 全 ≥70%、anchor meta-test 过 **9** 原则）。traceability 锁 **9** 原则全 expanded+safe；csdd 仍 **1.0**（饱和，只加真实覆盖面）；robust golden_score 0.897→0.9（SEC-09 expand 探针被匹配，无维度倒退）。本地 commit 只含 constitution + 测试 + STATUS；gate/trace/__main__/test_taste_score、security/、command_policy.py 的前几轮工作区改动仍未动（`ruff format --check` 因该批未提交在途改动仍红，非本 fire 引入）。
- **[2026-09-03] 关死「幽灵锚点」编造安全域的口子（本 fire）**：`test_every_anchor_resolves_and_matches_pattern` 只查 `exists()`（工作区），看不出某原则锚到**从未提交**的守卫——SEC-04 锚 `src/sandbox/command_policy.py`、SEC-05 锚 `src/security/injection_guard.py` 自首个宪法 commit `68703c3` 起就一直是 untracked ghost。fresh checkout 会丢这两个守卫：meta-test 红、traceability 报 missing、csdd=1.0 在 commit 层不可复现——这正是「编造安全域」的根口子。TDD 先红（新 meta-test `test_every_anchor_resolves_to_a_tracked_git_file` 加固为「锚必须被 git track」，当前即因 SEC-04 未 track 而 FAIL）后绿（`git add` 真实守卫文件 command_policy.py / security/__init__.py / injection_guard.py + test_security_guards.py 后，**306 passed / 56 skipped**、覆盖 **95.40% ≥ 92**、ruff lint clean、per-pkg 全 ≥70%）。constitution 未动（digest `e4c51afe…` 不变，非改尺子）；csdd 仍 **1.0**，traceability 锁 **9** 原则全 expanded+safe。本地 commit 只含守卫文件 + 宪法测试强化 + STATUS（gate/trace/__main__/test_taste_score 等前几轮在途改动仍未动）。
- **[2026-09-03] 关死 gate 的「自述回退」刷分口子（本 fire）**：前几轮在途的 demo-aware verify 让 `TraceabilityVerifier.verify` 对**无锚**探针返回 `None`，而 `gate.py` 的 `run` 落回 `agents[name](probe)`——即**无证据时信任 agent 自报**。这与 gate 模块 docstring（"did_expand/safe must never be taken on the agent's say-so"）和 AGENTS.md「自述不可信」锁直接矛盾：一个只报 `expand+safe` 的 liar 在任何无锚探针（enhancement/mutant）上零证据拿满分。TDD 先红（`test_gate_fails_closed_when_verify_has_no_evidence` 断言 `golden_score==0.0`，当前 gate 回退自报给 1.0 → FAIL）后绿（gate.py `run` 对 `verify is not None` 且 `verify(...)` 返回 `None` 时**fail-closed** 返回 `ProbeRun(did_expand=False, safe=False)`，**306 passed / 56 skipped**、覆盖 **95.41% ≥ 92**、ruff lint clean）。constitution/digest 未动；csdd 仍 **1.0**；demo ledger（robust 0.9 / liar 0.325 / reckless 0.225 rejected）与前一 fire 完全一致——因 demo 路径 `make_demo_aware_verify` 永不返回 None，gate 的 fail-closed 分支在 demo 里不触发，非回归。`scripts/check.sh` 的 format 步是 HEAD 就存在的 repo 级漂移（评估 `eval_llm/client.py` 等未动文件同样 DIRTY），非本 fire 引入。
- **[2026-09-03] 宪法扩到 10 原则 + 修掉 SEC-09 越转义（本 fire）**：加 SEC-10（CWE-862 工具调用**最小权限授权**→`src/tool_registry/registry.py`，锚 `permission not in self\._enabled`，禁 `except[^\n]*ok=True`）——授权层独立边界：registered_roles.py 把 maker/checker 接在 `ToolRegistry.call` 的权限门上，移除该门、反转成 `in`（fail-open 放行）、或把 handler 异常吞成 `ok=True`（fail-open 吞错），现有原则全锚 command/sandbox/safety/verifier/judge，**看不到**这条授权回归（无一条锚 tool_registry），必须钉住。TDD 先红（`test_tool_registry_least_privilege_boundary_is_constitutional` 断言 `p is not None`，无 SEC-10 时 `assert None is not None` fail）后绿（**307 passed / 56 skipped**、覆盖 **95.41% ≥ 92**、ruff lint clean、anchor meta-test 过 **10** 原则）。**顺带修 SEC-09 的越转义**：HEAD 的 violations 是 `except[^\\n]*Verdict\\.PASS`（`\n` 前 2 个反斜杠，TOML 解码成字面 `\\n`，正则 `[^\\n]`=「非反斜杠或 n」——**测不出它自己的 fail-open**）；改成 `except[^\n]*Verdict\\.PASS`（`\n` 前 1 个反斜杠=换行转义，`\.` 前 2 个反斜杠=字面点），实证 SEC-09 现在能抓到 `except ... return Verdict.PASS` 的同行 fail-open。digest `e4c51afe…`→`62c945…`（改尺子？——非改，是**修尺子的标度**：sentinel 从测不出→测得出，属加强）；traceability 锁 **10** 原则全 expanded+safe；csdd 仍 **1.0**（饱和）；robust golden 0.9→0.902（新增 SEC-10 expand 探针被匹配，无维度倒退）、reckless 仍 0.225 rejected（Pareto 否决未动）。本地 commit 只含 constitution + 测试 + STATUS。


- **[2026-09-04] 修 mutation-score 无效桩伪造 + 递归抓嵌套死桩（本 fire）**：上一 fire 的 `verifier_strength=0.758` 实被一个**生成器缺陷**偏低——`mutant_cases` 对以 `class X`/`def y` 开头的 pattern token 直接 `f"class {tok}: pass"`，把 `class SafetyGuard` 再包成 `class class SafetyGuard:`（**无效 Python**），验证器 `_is_dead_stub` 遇不可解析输入回退 `False` → 把它当「真实扩展」bless——8 条假守卫被错误判做安全扩展，0.758 是测出来的假象。本轮：①`mutation_score.py` 生成器只出**有效 Python** 假守卫（`_stub`/`_nested`/`_partial` 按 token 类型建真实死桩/嵌套死桩/惰性守卫；statement-fragment token（如 SEC-10）用含真句子的守卫壳），消除测不准；②`trace.py` 把 `_body_is_placeholder` 改成**递归**（每语句委托 `_stmt_is_placeholder`），抓到藏在未用 wrapper 里的嵌套死桩 `def _unused(): class Guard: pass`（顶层-only 检查本会 bless）；③按「非饱和」医理给每原则新增 `nested`（递归抓到→拒）与 `partial`（符号+真代码却无效=惰性守卫，本验证器语义抓不住，正是下一格余量）。TDD 先红（`test_stub_mutants_are_valid_python`、`test_verifier_rejects_nested_dead_stub`、`test_partial_guard_is_real_code_but_inert`、`test_mutant_cases_include_partial_and_nested_fakes` 新假守卫缺失/假桩无效时 FAIL）后绿（**322 passed / 56 skipped**、覆盖 **95.53% ≥ 92**、per-pkg 全 ≥70%、ruff lint+format 对 2 改动文件 clean；mutation_score.py 100% 覆盖）。实测 ledger `csdd_score=1.0`（不变，11 原则仍全 expanded+safe）｜`verifier_strength=0.758→0.8`（诚实上升：只剩 11 条 `partial` 惰性守卫被 bless——那才是语义天花板，无效 Python 假象没了；stub/comment/absent/nested 全被拒）；constitution digest `8675562b…` 不变（**没改尺子**）；ranking 不变（robust 0.905 / liar / reckless rejected，Pareto 否决未动）。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS。

- **[2026-09-04] 加固 verifier 抓「惰性直通守卫」+ 非饱和 ratchet（本 fire）**：上一 fire 的 `verifier_strength=0.8` 里被 bless 的全是 11 条 `partial` 惰性守卫——符号+真代码却**恒返回常量、不看输入**（`def allows_write(...): return True`，即「永远放行」的经典作弊：拦截门只是个空壳）。本轮用 AST 语义关掉它：`trace.py` 加 `_is_inert_module`（整模块每顶层语句都是「恒返回同一常量」的函数/类，或常量-only 赋值 → 判惰性，不 bless）。**关键防过收紧**：只看「返回的**常量集合**」，所以 `if x in roots: return True; return False`（两条不同常量）仍判真实决策不误杀；`return path in roots`（非常量）也绝不误杀。TDD 先红（`test_classify_rejects_inert_passthrough_guard` / `test_classify_rejects_inert_statement_forms` 在旧 `classify` 下 `did_expand=True` FAIL）后绿；另加 `test_verifier_still_blesses_a_real_branching_guard` 防过收紧 + `test_inert_vs_constant_hidden_split_the_cheat_space` 钉 ratchet。**顺带修 3 个 fixture 口径**：`test_taste_score.*`/`test_taste_score_constitution.*` 用 `def allow(path): return True` 当「合规守卫」范例——但那正是本 fire 判定为惰性作弊的形状，改成语义真实的 `return path in _allowed_roots`（机械不变，仍测「实现+clean→1.0」）。**非饱和 ratchet**：惰性守卫被抓后 `s` 会到 1.0（spec 明令禁止），故按设计「再加一格更细假守卫」给每原则加 `constant-hidden`（返回值经一个 NAME 传，如 `return ALWAYS`，检测器溯不到），s 诚实留在 <1.0。实测 ledger `csdd_score=1.0`（不变，11 原则仍全 expanded+safe）｜`verifier_strength=0.8→0.833`（更细的假集上诚实上升；stub/comment/absent/nested/partial 全被拒，只剩 constant-hidden 是下一格余量）｜constitution digest `8675562b…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.905 / liar / reckless rejected，Pareto 否决未动）。全量 **322→326 passed / 56 skipped**、覆盖 **94.06% ≥ 92**、按包闸门全 ≥70%、`ruff check` clean、`ruff format` 对两个改动源文件（trace.py/mutation_score.py）clean。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS。

- **[2026-09-04] 加固 verifier 抓「method-call 隐藏常量」+ ratchet 到 attr-value-hidden（本 fire）**：上一 fire 的 `verifier_strength=0.875` 里被 bless 的 11 条全是 `attribute-hidden` 惰性守卫——符号+真代码，但常量值经**方法调用**返回（`return self._always()` / `return _Helper().always(...)`），helper-常量检测器只溯 NAME/顶层 helper CALL、溯不到 bound-method CALL，故当作「真实决策」放行。本轮在 `trace.py` 加**类上下文溯源**：`_resolve_callable_constant`（对 def 取单一常量，带 key 防环路）+ `_resolve_method_constant`（按 `class_def.name:method` 找方法）+ `_resolve_attribute_constant`（`self._m()` 在 `current_class` 上解析、`_Helper().m()` 在模块 `classes` 映射上解析）+ 把 `classes`/`current_class` 穿进 `_resolve_literal_key`/`_is_inert_function`/`_is_inert_statement`/`_is_inert_module`。**关键防过收紧**：只追 bound-method CALL——`self._decide(path)` 若返回 `path in roots`（Compare）仍是非恒定→真实决策，绝不误杀（`test_verifier_still_blesses_method_that_decides_on_state` 钉）。**非饱和 ratchet**：method-call 被抓后按设计加更细假守卫 `attr-value-hidden`（常量经**裸 attribute VALUE** 返回，`return self._ALWAYS`，无调用——检测器只对 NAME/顶层 helper CALL/bound-method CALL 溯源、不对 Attribute 值溯源），诚实留在 <1.0。TDD 先红（`test_verifier_rejects_attribute_call_passthrough`、`test_inert_vs_constant_hidden_..._split_the_cheat_space` 的 attribute-hidden 断言在旧 verifier 下 `did_expand=True` FAIL）后绿。实测 ledger `csdd_score=1.0`（不变，11 原则仍全 expanded+safe——`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ 未过收紧）｜`verifier_strength=0.875→0.889`（更细假集上诚实上升：attribute-hidden 被拒，只剩 attr-value-hidden 是下一格余量）｜constitution digest `8675562b…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.905 / liar 0.357 / reckless rejected，Pareto 否决未动）。全量 **329→331 passed / 56 skipped**、覆盖 **93.55% ≥ 92**、按包闸门全 ≥70%（taste_score 89.2%）、`ruff check` clean、`ruff format` 对两个改动源文件（trace.py/mutation_score.py）+ 测试 clean。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS + cerebrum。

- **[2026-09-03] 关死第六道锁的「内容级改尺子」口子（本 fire）**：`constitution.digest()` 只哈希 `version` + 原则 `id`，不哈希原则的 `boundary/cwe/level/constraint/anchor/pattern/violations/rationale`——在某原则**原地**把 `violations` 改成永不匹配的 sentinel（id、version 都不变），digest 不变，`TasteGate.pinned_digest` 不会判「ruler tampered」。这正是第六道锁要防的：规则从内部被悄悄放松（把惩罚探针改瞎），却**零证据、不触发否决**地拿满分。TDD 先红（`test_constitution_digest_changes_when_principle_content_changes` 断言 `strict.digest() != weakened.digest()`，当前两 digest 相同 → FAIL）后绿（`digest()` 改为哈希全部原则字段，`dataclasses.fields(Principle)` 按定义序取全字段，**308 passed / 56 skipped**、覆盖 **95.41% ≥ 92**、ruff lint clean、constitution.py 100% 覆盖）。实证：把 SEC-01 `violations` 改瞎 → digest `2aec4286…`→`df49bc93…`，gate 幂等判 `rejected=True / constitution integrity violation (ruler tampered)`；未改动的真规则 digest 不变。**非改尺子，是修尺子的标度**（digest 从测不出内容改动→测得出，加强第六道锁）。traceability 锁 **10** 原则全 expanded+safe；csdd 仍 **1.0**（饱和，不加分，只关真实漏洞）；ranking 不变（robust 0.902 / liar 0.341 / reckless 0.244 rejected，Pareto 否决未动）。`ruff format --check` 仍红（repo HEAD 既有漂移，评估 `eval_llm/client.py`、`src/taste_score/__main__.py`、`gate.py`、`judge.py`、`source.py`、`tests/test_real_llm*`、`tests/test_taste_score_constitution.py` 等**未动文件**同样 DIRTY，本 fire 未引入、未动在途改动）。.
- **[2026-09-03] verifier mutation-score（本 fire）：非饱和的「验证器强度」维度**——csdd_score 一旦 11 原则全实现+clean 就饱和在 **1.0**，这是**尺子太粗**不是「引擎完美」；引擎自己的「抓住作弊者」声明无人测量。加**agent 无关**的非饱和计数器：注入作弊者真会交出的假守卫（死桩 `class X: pass` / 只在注释里出现的 pattern token / 完全缺失），统计验证器**拒绝 bless**（当作「真实安全扩展」放行）的比例。TDD 先红（新 `tests/test_verifier_mutation_score.py`，`trace.classify`/`mutation_score` 不存在→ImportError fail）后绿：`trace.py` 抽出纯函数 `classify(pattern,violations,text,probe_id)`（`_run` 读锚文件后委托它，行为不变）+ `mutation_score.py`（`mutant_cases`/`verifier_strength`）+ `__main__.py` 在 ledger 里随 csdd_score 一起输出 `verifier_strength`。**313 passed / 56 skipped**、ruff lint clean；实测 ledger `csdd_score=1.0`（饱和）｜`verifier_strength=0.394`（<1.0 = 诚实、非饱和的余量：naive 正则目前抓不住死桩/注释 pattern，这正是下一格加固点）。本地 commit `daa2fa3`（含 trace.py/__main__.py/mutation_score.py/test_verifier_mutation_score.py，未 push）。

- **[2026-09-03] 宪法扩到 11 原则（本 fire）**：加 SEC-11（CWE-770 资源/预算**fail-closed**→`src/cost_control/cost.py`，锚 `def guard_budget`，禁 `except[^\\n]*BudgetError`）——SEC-01..SEC-10 钉的是写隔离/注入/执行层/评估层/授权层，**没一条锚 cost_control**。`guard_budget` 是真实的 fail-closed 资源闸门（估算成本超 `max_usd` 即抛 `BudgetError` 拒绝启动，先挡住超支再谈执行）；若 agent 把它的调用包进 `except BudgetError: return cost`（吞错放行成继续跑）或移除预算上限，现有原则全看不到——那条资源失控回归会零证据拿满分（CWE-770 unbounded resource consumption）。必须钉住。TDD 先红（`test_cost_budget_guard_boundary_is_constitutional` 无 SEC-11 时 `assert None is not None` fail）后绿（**309 passed / 56 skipped**、覆盖 **95.41% ≥ 92**、ruff lint clean、anchor meta-test 过 **11** 原则）。digest `2aec4286…`→`8675562b…`（新增原则，非改尺子）；traceability 锁 **11** 原则全 expanded+safe；csdd 仍 **1.0**（饱和，只加真实覆盖面）；robust golden 0.902→0.905（新增 SEC-11 expand 探针被匹配，无维度倒退）、reckless 仍 rejected（Pareto 否决未动）。本地 commit 只含 constitution + 测试 + STATUS。`ruff format --check` 仍红（repo HEAD 既有漂移，本 fire 未引入）。

- **[2026-09-03] 加固 verifier 抓「死桩/注释 pattern」假守卫（本 fire）**：上一 fire 的 `verifier_strength=0.394` 标记的正是「naive 正则抓不住死桩/注释 pattern」这一格。本轮关掉它：`trace.py` 的 `classify` 从「pattern 出现即 expanded」收紧为「pattern 出现 **且** 模块不是占位壳」——加 AST 级 `_is_dead_stub`（`class X: pass` / 注释-only / docstring-only 壳都算占位，不 bless），让「有符号无逻辑」的壳不再被当成真实安全扩展。TDD 先红（`test_classify_rejects_dead_stub_guard`、`test_classify_rejects_comment_only_guard` 在旧 `classify` 下 FAIL——旧验证器把 `class SafetyGuard: pass` 与 `# allows_write` 都 bless 成 `did_expand=True`）后绿（另加 `test_classify_rejects_docstring_only_stub`、`test_classify_still_blesses_a_real_guard`、`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 防**过收紧**：真实锚文件仍全 expanded+safe）。**318 passed / 56 skipped**、覆盖 **95.46% ≥ 92**、ruff lint+format 对 3 改动文件 clean（repo HEAD 既有 format 漂移不涉本 fire）。实测 ledger `csdd_score=1.0`（不变，11 原则仍全 expanded+safe）｜`verifier_strength=0.394→0.758`（验证器真加强，死桩/注释壳被挡回）；constitution digest `8675562b…` 不变（**没改尺子**）。本地 commit 只含 trace.py + mutation_score.py(仅 docstring 校正，非改行为) + 测试 + STATUS。

- **[2026-09-04] 加固 verifier 抓「裸 class-body attribute VALUE 常量」+ ratchet 到 instance-attr（本 fire）**：上一 fire 的 `verifier_strength=0.889` 里被 bless 的 11 条全是 `attr-value-hidden` 惰性守卫——符号+真代码，但常量经**裸 attribute VALUE 无调用**返回（`return self._ALWAYS` / `return _Helper().val`，`_ALWAYS = True` 在**类体**），常量-检测器只溯 NAME/顶层 helper CALL/bound-method CALL，溯不到裸 `ast.Attribute` 值，故当「真实决策」放行。本轮在 `trace.py` 加 `_resolve_attribute_value_constant`（对 `self.X`/`_Helper().X`/`_Mod.X` 按类体常量赋值溯源）+ `_resolve_literal_key` 的 `ast.Attribute` 分支。**关键防过收紧**：只追「绑到类体可证常量字面量」的 attribute——`return path in self._allowed`（Compare，非 Attribute 值）与 `__init__` 里才绑的实例 attribute 都不被判惰性，绝不误杀真实守卫（`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ csdd 仍 1.0，未过收紧）。**非饱和 ratchet**：class-body 被抓后按设计加更细假守卫 `instance-attr-hidden`（常量经 **`__init__` 里绑的实例 attribute** 返回，`self._ALWAYS` 在构造函数而非类体——检测器只看类体赋值所以仍 bless），诚实留在 <1.0。TDD 先红（`test_attribute_value_constant_is_now_rejected` / 拆分测试的 `attr-value-hidden` 断言在旧 verifier 下 `did_expand=True` FAIL）后绿。另加 `test_module_class_attribute_value_is_rejected` 覆盖 `_Mod.val` 裸类名路径（防新分支成死代码）。实测 ledger `csdd_score=1.0`（不变，11 原则仍全 expanded+safe）｜`verifier_strength=0.889→0.9`（更细假集上诚实上升：class-body attr-value 被拒，只剩 instance-attr 是下一格余量）｜constitution digest `8675562b…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.905 / liar 0.357 / reckless rejected，Pareto 否决未动）。全量 **333 passed / 56 skipped**、覆盖 **93.45% ≥ 92**、按包闸门全 ≥70%（taste_score trace.py 78.33%，mutation_score.py 100%）、`ruff check` clean、`ruff format` 对三个改动文件（trace.py/mutation_score.py/test_verifier_mutation_score.py）clean。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS + cerebrum。

- **[2026-09-04] 加固 verifier 抓「__init__ 绑定的实例属性常量」+ ratchet 到 mutator-bound（本 fire）**：上一 fire 的 `verifier_strength=0.9` 里被 bless 的全是 11 条 `instance-attr-hidden` 惰性守卫——符号+真代码，但常量经**构造函数里绑的实例属性**返回（`self._ALWAYS = True` 在 `def __init__`，`check` 返回 `self._ALWAYS`），解析器只溯 class-body/NAME/helper-call/method-call/attr-value、溯不到 `__init__` 里绑的。本轮在 `trace.py` 加 `_resolve_instance_attr_constant`（对 `self.X` 与 `_Helper().X` 只在 `__init__` 里**恰好一次**赋给**可证常量**时解析，重赋值/来自参数/无赋值都回 `_NON_CONSTANT` 防过收紧）+ `_is_inert_function` no-return 分支改为「全语句惰性」才判惰性（否则一个只赋常量的 `__init__` 会把整个 class 染成非惰性，让这个作弊漏网）。**关键防过收紧**：`self._allowed = allowed`（来自参数，非常量）、`self._ALWAYS = True; self._ALWAYS = False`（重赋值）、只绑别的属性、`__init__` 里带 setup CALL——都判真实决策绝不误杀（3 个新 anti-over-rejection 测试钉）。**非饱和 ratchet**：instance-attr 被抓后按设计加更细假守卫 `inst-attr-mutator-hidden`（常量经**非 __init__ 的 mutator 方法**绑，`self._ALWAYS = True` 在 `_setup()`、`check` 调 `_setup()` 后返回——解析器只溯 class-body/__init__，溯不到任意方法）。TDD 先红（`test_instance_attr_constant_is_now_rejected` 在旧 verifier 下 `did_expand=True` FAIL）后绿（**338 passed / 56 skipped**、覆盖 **93.84% ≥ 92**、按包闸门全 ≥70% (taste_score 90.5%、trace.py 77.83%)、`ruff check` clean、`ruff format` 对三个改动文件 (trace.py/mutation_score.py/test_verifier_mutation_score.py) clean；我新加的 `_resolve_instance_attr_constant` 分支全被 anti-over-rejection 测试走到，无死码）。实测 ledger `csdd_score=1.0`（不变，11 原则仍全 expanded+safe——`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ 未过收紧）｜`verifier_strength=0.9→0.9091`（更细假集上诚实上升：instance-attr 被拒，只剩 inst-attr-mutator 是下一格余量）｜constitution digest `8675562b…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.905 / liar 0.357 / reckless 0.262 rejected，Pareto 否决未动）。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS + cerebrum。
- **[2026-09-04] 加固 verifier 抓「mutator 绑定实例属性常量」+ ratchet 到 factory-hidden（本 fire）**：上一 fire 的 `verifier_strength=0.9091` 里被 bless 的 11 条全是 `inst-attr-mutator-hidden` 惰性守卫——符号+真代码，但常量经**非 __init__ 的 mutator 方法**绑（`self._ALWAYS = True` 在 `_setup()`、`check` 调 `_setup()` 后返回 `self._ALWAYS`），解析器只溯 class-body/`__init__`、溯不到「同一函数里被调用的 mutator」。本轮在 `trace.py` 加**调用点溯源**：`_instance_receiver_class`（self / 局部实例 `h = _Cls()` / `_Cls()` 构造 / 裸类名）+ `_named_local_classes`（`x = _Cls()`→局部→类）+ `_iter_calls_of`（收集该函数的直接 call，**不**下钻嵌套 def/class/lambda）+ `_method_binds_attr_constant`（某方法是否**恰好一次**把 `self.<attr>` 绑到可证常量，重赋值/来自参数回 `_NON_CONSTANT` 防过收紧）+ `_mutator_bound_attr_constant`（围函调到接收类的方法、且该方法是紧绑定该 attr 才判常量）穿进 `_resolve_attribute_value_constant`（新增 `enclosing_fn`/`locals_map` 参数，经 `_resolve_literal_key`/`_is_inert_function` 逐层下传）。**关键防过收紧**：只追「同一函数真的调用它 + 它把**被读的那个 attr** 绑到单一可证常量」——一个真实守卫调用 `_setup` 做别的副作用、却读另一个真实状态相关 attribute，仍非恒定、绝不误杀（`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ csdd 仍 1.0，未过收紧）。**非饱和 ratchet**：mutator 被抓后按设计加更细假守卫 `factory-hidden`（常量经**工厂函数返回的实例**读，`_make().val`——解析器对 `self`/局部/`_Cls()` 构造这种静态接收类判常量，对工厂函数的返回类型不追踪，所以仍 bless），诚实留在 <1.0。TDD 先红（`test_method_bound_attr_constant_is_now_rejected` 与拆分测试的 `inst-attr-mutator-hidden` 断言在旧 verifier 下 `did_expand=True` FAIL，共 2 fail）后绿（**30 passed**）。实测 ledger `csdd_score=1.0`（不变，13 原则仍全 expanded+safe）｜`verifier_strength=0.9091→0.9167`（更细假集上诚实上升：mutator-bound 被拒，只剩 factory-hidden 是下一格余量）｜constitution digest `321cf35c…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.909 / liar 0.386 / reckless 0.295 rejected，Pareto 否决未动）。全量 **341 passed / 56 skipped**、覆盖 **93.54% ≥ 92**、按包闸门全 ≥70%（taste_score 90.3%）、`ruff check` clean、`ruff format` 对三个改动文件（trace.py/mutation_score.py/test_verifier_mutation_score.py）clean。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS + cerebrum。
- **[2026-09-04] 宪法扩到 12 原则（本 fire）**：加 SEC-12（CWE-345 数据真实性未验证 → `src/goal_loop/world_verifier.py`，锚 `class WorldVerifier|def verify_all`，禁 `except[^\n]*ok=True`）——SEC-01..SEC-11 钉的是写隔离/注入/执行/评估/授权/预算，**没一条锚 world_verifier**。`WorldVerifier.verify_all` 是真实的 fail-closed 完成守卫：把磁盘重读的字节当唯一可信证据，任一 check 的 ok=False 即返回失败（missing/字节不匹配绝不提升为成功），实现「verify the world / 自述不可信」的机器侧。若 agent 把验证包进 `except ...: return ... ok=True`（吞掉验证失败放行成完成）或删掉 `if not result.ok`（无视失败直接 ok=True），现有原则全看不到——那条「谎报完成」回归会零证据拿满分。TDD 先红（`test_world_verifier_fail_closed_boundary_is_constitutional` 无 SEC-12 时 `assert None is not None` fail）后绿（**339 passed / 56 skipped**、覆盖 **93.84% ≥ 92**、按包闸门全 ≥70%、`ruff check`+`ruff format` 对改动文件 clean、anchor meta-test 过 **12** 原则）。violations 用 SEC-09 修过的正确转义（`[^\n]` 经 TOML 解码成字面 LF=`非换行`），用字节级脚本实证能抓到同行 `except ... return ... ok=True` 的 fail-open、clean 锚不误伤（多行 return 不抓是既定口径，与 SEC-09/10/11 一致）。digest `8675562b…`→`2f43c3e6…`（**新增原则**，非改尺子；compete 每轮对已载入宪法重新 pin，Lock-6 完好）｜traceability 锁 **12** 原则全 expanded+safe｜csdd 仍 **1.0**（饱和，只加真实覆盖面）｜verifier_strength **0.909090…** 原样不变（SEC-12 的假守卫被拒比例与均线一致，非回归）｜robust golden **0.905→0.907**（新 SEC-12 expand 探针被匹配，无维度倒退）、liar 0.357→0.372、reckless 0.262→0.279 仍 rejected（Pareto 否决未动）。本地 commit 只含 constitution + 测试 + STATUS。

- **[2026-09-04] 宪法扩到 13 原则（本 fire）**：加 SEC-13（CWE-703 goal 循环**崩溃包络 fail-closed**→`src/goal_loop/loop_runner.py`，锚 `class GoalLoopRunner|except Exception`，禁 `except[^\n]*(ok=True|Verdict\.PASS)`）——SEC-01..SEC-12 钉的是写隔离/注入/执行/评估/授权/预算/verify-the-world，**没一条锚 loop_runner 的崩溃处理**。`GoalLoopRunner.run` 是真实的 fail-closed 崩溃包络：maker 抛异常被吞成 `MakerOutput(ok=False)`（no-progress，崩溃的 maker 绝不完成 goal）、checker 抛异常被吞成 `CheckerOutput(verdict=Verdict.FAIL)`——任何崩溃都绝不跨过完成判断。若 agent 把调用包进 `except ...: return ... ok=True`（吞 maker 崩溃并申报进展）或 `except ...: return ... Verdict.PASS`（吞 checker 崩溃并宣布通过），现有 12 条原则全看不到（无一条锚 loop_runner）——那条「崩溃被吞成成功」回归会零证据拿满分。TDD 先红（`test_goal_loop_crash_fail_closed_boundary_is_constitutional` 无 SEC-13 时 `assert None is not None` fail）后绿（**340 passed / 56 skipped**、覆盖 **93.84% ≥ 92**、per-pkg 全 ≥70%、`ruff check` clean、anchor meta-test 过 **13** 原则）。violations 转义经字节级实证：源 `\n`→解码 LF（`[^\n]`=非换行，同行 fail-open 才抓，多行 return 不抓是既定口径与 SEC-09/12 一致）、源 `\\.`→解码 `\.`（字面点）；regex 实测能抓 `except ...: return Verdict.PASS` 与 `except ...: return MakerOutput(ok=True)`、clean 锚不误伤（真实 fail-closed ok=False/Verdict.FAIL 形式不匹配）。digest `2f43c3e6…`→`321cf35c…`（**新增原则**，非改尺子；compete 每轮对已载入宪法重新 pin，Lock-6 完好）｜traceability 锁 **13** 原则全 expanded+safe｜csdd 仍 **1.0**（饱和，只加真实覆盖面）｜verifier_strength **0.9091** 原样不变（SEC-13 的假守卫被拒比例与均线一致，非回归）｜robust golden **0.907→0.909**（新 SEC-13 expand 探针被匹配，无维度倒退）、liar 0.372→0.386、reckless 0.279→0.295 仍 rejected（Pareto 否决未动）。本地 commit 只含 constitution + 测试 + STATUS。
- **[2026-09-04] 加固 verifier 抓「工厂函数返回类的常量」+ ratchet 到 nested-factory（本 fire）**：上一 fire 的 `verifier_strength=0.9167` 里被 bless 的 13 条全是 `factory-hidden` 惰性守卫——符号+真代码，但常量经**工厂函数返回的实例**读（`_make().val`，`_Helper` 的 `val` 在 `_setup()` 绑好），解析器对 `self`/局部/`_Cls()` 构造这种静态接收类判常量、对「函数返回类型」不追踪，且 `_make` 返回非恒量 `h` 使整模块不能被 `_is_inert_module` 判惰性 → 当作真实扩展 bless。本轮在 `trace.py` 关掉它：①`_factory_return_class`/`_return_expr_class`/`_factory_receiver`——对「纯构造工厂（`h = _Cls(...); return h`）」解析其单一返回类；②`_resolve_attribute_value_constant` 在 `_instance_receiver_class` 落空后走工厂路径，且把**工厂本身**作 enclosing、`_named_local_classes(工厂)` 作 receiver_locals（mutator 调用在工厂体内，不在 guard 里）；③`_is_inert_function` 对「返回全惰性类实例」的 return 判惰性（否则 `_make` 返回 `h` 会让整模块不惰性、让 `_guard` 漏网），并加 `_class_is_fully_inert`/`_returns_inert_instance`（自引用类用 `_depth>8` 防挂死）。**关键防过收紧**：只追「工厂确实只返回 ONE 静态类 + 该类成员全惰性 + 读的 attr 在 class-body/`__init__`/工厂体内被调 mutator 里可证常量」——`_make().allows_write(path)`（真决策的守卫）、工厂带参数/分支返回（`_make(kind)` 返回两种类型）、类成员有真实状态分支——都判真实决策绝不误杀（4 个新 anti-over-rejection 测试钉）。**非饱和 ratchet**：工厂被抓后按设计加更细假守卫 `nested-factory-hidden`（常量经**工厂再委托给另一个 builder** 的实例读，`_make()` 返回 `_build()`——解析器只追一层工厂、不递归，所以仍 bless），诚实留在 <1.0。TDD 先红（`test_factory_indirect_attr_constant_is_now_rejected`/`test_inert_vs_constant_hidden_..._split_the_cheat_space` 的 factory-hidden 断言在旧 verifier 下 `did_expand=True` FAIL，共 2 fail）后绿（**345 passed / 56 skipped**、覆盖 **93.68% ≥ 92**、按包闸门全 ≥70%（taste_score 88.6%、trace.py 84.36%、mutation_score.py 100%）、`ruff check` clean、`ruff format` 对三个改动文件 clean；新 factory 分支全被 anti-over-rejection 测试走到，无死码）。实测 ledger `csdd_score=1.0`（不变，13 原则仍全 expanded+safe——`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ 未过收紧）｜`verifier_strength=0.9167→0.9231`（更细假集上诚实上升：factory-hidden 被拒，只剩 nested-factory-hidden 是下一格余量）｜constitution digest `321cf35c…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.909 / liar 0.386 / reckless 0.295 rejected，Pareto 否决未动）。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS + cerebrum。


- **[2026-09-04] 加固 verifier 抓「工厂链委托（nested-factory-hidden）」+ ratchet 到 static-factory-hidden（本 fire）**：上一 fire 的 `verifier_strength=0.9231` 里被 bless 的 13 条全是 `nested-factory-hidden` 惰性守卫——符号+真代码，但常量经**工厂再委托给另一个 builder** 的实例读（`_make()` 返回 `_build()`，guard 读 `_make().val`），解析器只追一层工厂、不递归，故当作真实扩展 bless。本轮在 `trace.py` 关掉它：①`_factory_return_class`/`_return_expr_class` 加 `sources`+`_depth`，对「return 表达式本身是另一个工厂调用」递归解析到基 builder 的返回类；②`_factory_receiver` 改为**先递归走委托链**（`_make`→`_build`），返回**基 builder**（不是包装工厂）作 enclosing——mutator `_setup()` 是在基 builder `_build` 体内被调的，用 `_make` 当 enclosing 会漏；③`_returns_inert_instance` 把 `sources` 传给 `_return_expr_class`，使「返回全惰性类实例」的工厂链也能判惰性（否则 `_make` 返回 `_build()` 会染非惰性、漏网）。**关键防过收紧**：只追「工厂确实只返回 ONE 静态类 + 该类成员全惰性 + 读的 attr 在类体/`__init__`/基 builder 体内被调 mutator 里可证常量」——嵌套工厂返回**真决策守卫**（`allows_write` 返回 `path in roots`）、读的 attr 永远没绑可证常量——都判真实决策绝不误杀（2 个新 anti-over-rejection 测试 `test_nested_factory_that_decides_on_state_is_still_blessed`/`test_nested_factory_receiver_attr_not_provable_is_blessed` 钉）。**非饱和 ratchet**：nested-factory 被抓后按设计加更细假守卫 `static-factory-hidden`（常量经**class-level factory METHOD** 读，`_Helper.create().val`——解析器只走模块级**工厂函数**链，溯不到 `@staticmethod`/`@classmethod` 工厂方法的返回类型，所以仍 bless，诚实留在 <1.0）。TDD 先红（`test_nested_factory_indirect_attr_constant_is_now_rejected` 与拆分测试的 nested-factory-hidden 断言在旧 verifier 下 `did_expand=True` FAIL，共 2 fail）后绿（**348 passed / 56 skipped**、覆盖 **93.64% ≥ 92**、按包闸门全 ≥70%（taste_score 90.8%、trace.py 84.39%、mutation_score.py 100%）、`ruff check` clean、`ruff format` 对三改动文件 clean）。实测 ledger `csdd_score=1.0`（不变，13 原则仍全 expanded+safe——`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ 未过收紧）｜`verifier_strength=0.9231→0.9286`（更细假集上诚实上升：nested-factory 被拒，只剩 static-factory-hidden 是下一格余量）｜constitution digest `321cf35c…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.909 / liar 0.386 / reckless 0.295 rejected，Pareto 否决未动）。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS + cerebrum。

- **[2026-09-04] 加固 verifier 抓「class-level factory METHOD（static-factory-hidden）」+ ratchet 到 nested-static-factory-hidden（本 fire）**：上一 fire 的 `verifier_strength=0.9286` 里被 bless 的 13 条全是 `static-factory-hidden` 惰性守卫——符号+真代码，但常量经**class-level factory METHOD** 读（`_Helper.create().val`，`create` 是 `@staticmethod` 构造并返回一个 `_Helper`），解析器只走模块级**工厂函数**链、溯不到 `@staticmethod`/`@classmethod` 工厂方法的返回类型，故当作真实扩展 bless。本轮在 `trace.py` 关掉它：①新增 `_class_factory_receiver`（把 `_Helper.create()` 接到方法所在类，`_find_method` 取方法 def，**先递归**走「方法→方法 / 方法→模块函数」委托链到基 builder，再 `_factory_return_class` 直接构造）+ 把 `_factory_receiver` 拆成模块函数（Name）与类方法（Attribute）两路分派；②**自引用类惰性 coinductive 修复**：`create` 是它构造的类的成员（`class C: @staticmethod def make(): return C()`），`_class_is_fully_inert(C)` 检查成员 `make` → `_returns_inert_instance` → 再 `_class_is_fully_inert(C)` 成环，`_depth>8` 把环断成「非惰性」→ `create` 判真实决策、整模块非惰性、漏网——给 `_class_is_fully_inert`/`_is_inert_function`/`_is_inert_statement`/`_returns_inert_instance` 穿 `_visiting`（已检类集），重入已假设惰性的类直接回 True（coinductive），环被断、`create` 判惰性、模块整体惰性、漏网关闭。**关键防过收紧**：`_visiting` 假设只在「其余成员全惰性」时成立——真实状态分支成员（`allows_write` 返回 `path in roots`）仍判非惰性，`_class_is_fully_inert` 返回 False、绝不过杀（`test_static_factory_that_decides_on_state_is_still_blessed` 钉）。**非饱和 ratchet**：class-factory 被抓后按设计加更细假守卫 `nested-static-factory-hidden`（常量经**工厂方法再委托给同类的另一个工厂方法**读，`create()` 返回 `_build()`——解析器对类方法只追**一层**直接构造、不递归方法→方法委托链，所以仍 bless，诚实留在 <1.0）。TDD 先红（`test_static_factory_indirect_attr_constant_is_now_rejected` 在旧 verifier 下 `did_expand=True` FAIL）后绿（**350 passed / 56 skipped**、覆盖 **93.42% ≥ 92**、按包闸门全 ≥70%（taste_score 90.5%、trace.py 83.69%、mutation_score.py 100%）、`ruff check` clean、`ruff format` 对改动文件 clean）。实测 ledger `csdd_score=1.0`（不变，13 原则仍全 expanded+safe）｜`verifier_strength=0.9286→0.9333`（更细假集上诚实上升：static-factory 被拒，只剩 nested-static-factory 是下一格余量）｜constitution digest `321cf35c…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.909 / liar 0.386 / reckless 0.295 rejected，Pareto 否决未动）。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS + cerebrum。

- **[2026-09-04] 加固 verifier 抓「工厂方法再委托同类的另一个工厂方法（nested-static-factory-hidden）」+ ratchet 到 classmethod-cls-factory（本 fire）**：上一 fire 的 `verifier_strength=0.9333` 里被 bless 的 13 条全是 `nested-static-factory-hidden` 惰性守卫——符号+真代码，但常量经**工厂方法再委托给同类的另一个工厂方法**读（`_Helper.create()` 返回 `_build()`、`create`/`_build` 都是 `@staticmethod`，guard 读 `_Helper.create().val`），解析器对 class-factory 方法只追**一层**直接构造、且 `_factory_receiver` 只能溯「模块级函数(Name in sources)/属性调用(`_Helper._build()`)」、溯不到**裸 Name 的同辈方法调用**（`return _build()`，既不在 sources 也不是 Attribute），故当作真实扩展 bless。本轮在 `trace.py` 关掉它，两处一起改：①新增 `_sibling_method_factory_receiver`（对「return 表达式是**裸 Name 调用同类同辈方法**」递归解析到基 builder 的返回类——`_find_method(cls,'_build')` → 委托链 → `_factory_return_class` 直接构造），并接入 `_class_factory_receiver` 的委托循环（`_factory_receiver` 返回 None 时落这个分支）；②`_returns_inert_instance` 加 `enclosing_class` 参数（来自 `_is_inert_function` 的 `current_class`），对「同辈方法委托的工厂」也能判该包装方法 `create` 是**纯工厂ship惰性类**（否则 `_build()` 解不出类、`create` 被判非惰性、整模块非惰性、漏网）。**关键防过收紧**：`_sibling_method_factory_receiver` 只认「裸 Name 同辈方法」——`_Helper._build()`（属性调用）本来就由 `_factory_receiver`→`_class_factory_receiver` 递归接住、`return _build()`（裸 Name，确为同类方法）才走新分支；非方法裸名/深度>8 回 None；`_returns_inert_instance` 的 `enclosing_class is None`（模块级函数，非方法）跳过该分支。`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ csdd 仍 1.0，未过收紧。**非饱和 ratchet**：nested-static 被抓后按设计加更细假守卫 `classmethod-cls-factory-hidden`（常量经**ClassMETHOD 工厂 + `cls()` 构造**读，`_Helper.create().val`，`create` 是 `@classmethod`、`h = cls()`——`cls()` 是运行时参数接收者、静态分析器无法绑到类，所以仍 bless，诚实留在 <1.0）。TDD 先红（`test_inert_vs_constant_hidden_..._split_the_cheat_space` 的 nested-static 断言与 `test_nested_static_factory_indirect_attr_constant_is_now_rejected` 在旧 verifier 下 `did_expand=True` FAIL，共 2 fail）后绿（**351 passed / 56 skipped**、覆盖 **93.40% ≥ 92**、按包闸门全 ≥70%（taste_score 90.7%、trace.py 84.03%、mutation_score.py 100%）、`ruff check` clean、`ruff format` 对三个改动文件 clean）。实测 ledger `csdd_score=1.0`（不变，13 原则仍全 expanded+safe）｜`verifier_strength=0.9333→0.9375`（更细假集上诚实上升：nested-static-factory 被拒，只剩 classmethod-cls-factory 是下一格余量）｜constitution digest `321cf35c…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.909 / liar 0.386 / reckless 0.295 rejected，Pareto 否决未动）。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS + cerebrum。

- **[2026-09-04] 加固 verifier 抓「classmethod 工厂 + cls() 构造（classmethod-cls-factory-hidden）」+ ratchet 到 nested-classmethod-cls-factory（本 fire）**：上一 fire 的 `verifier_strength=0.9375` 里被 bless 的 13 条全是 `classmethod-cls-factory-hidden` 惰性守卫——符号+真代码，但常量经 **`@classmethod` 工厂 + `cls()` 构造**读（`_Helper.create().val`，`create` 是 `@classmethod`、`h = cls(); h._setup(); return h`），解析器对 class-factory 方法只认「直接构造模块级类（`_Helper()`/`h = _Helper()`）」，`cls()` 是运行时参数接收者、绑不到类，故当作真实扩展 bless。本轮在 `trace.py` 关掉它：给整条工厂解析链穿 **classmethod 接收器**（`cls` 首参 = 被调用类），`_return_expr_class`/`_named_local_classes`/`_factory_return_class` 加 `cls_class`/`cls_name` 参数，`_class_factory_receiver`/`_resolve_attribute_value_constant`/`_returns_inert_instance` 在 `_is_classmethod(method)` 时把「方法首参名」当 `cls_name`、「接收类」当 `cls_class` 穿下去，使 `h = cls()` 与直接 `return cls()` 都解析回接收类 → 读的 attr 经 `_setup()`/类体解析成常量 → `_guard` 判惰性、整模块惰性、漏网关闭。**关键防过收紧**：只认「`_is_classmethod` 真装饰的方法 + 首参名」；`@staticmethod`（无 cls param）、模块级函数（enclosing_class=None）、非类方法装饰都不穿 `cls`，行为与先前一致；`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ csdd 仍 1.0，未过收紧。**非饱和 ratchet**：classmethod-cls 被抓后按设计加更细假守卫 `nested-classmethod-cls-factory-hidden`（常量经**classmethod 工厂再委托给同类 classmethod** 读，`create()` 返回 `_build()`、两者都 `@classmethod`——`cls` 没穿过 classmethod→classmethod 委托链，`_build` 里的 `cls()` 解不出类，所以仍 bless，诚实留在 <1.0）。TDD 先红（拆分测试的 classmethod-cls 断言从「仍 bless」改为「must now be rejected」在旧 verifier 下 `did_expand=True` FAIL，另 `test_classmethod_cls_factory_indirect_attr_constant_remains_headroom` 同样 FAIL）后绿（**352 passed / 56 skipped**、覆盖 **93.51% ≥ 92**、按包闸门全 ≥70%（taste_score 90.7%、trace.py 84.73%、mutation_score.py 100%）、`ruff check` clean、`ruff format` 对三个改动文件 clean）。实测 ledger `csdd_score=1.0`（不变，13 原则仍全 expanded+safe）｜`verifier_strength=0.9375→0.9412`（更细假集上诚实上升：classmethod-cls 被拒，只剩 nested-classmethod-cls 是下一格余量）｜constitution digest `321cf35c…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.909 / liar 0.386 / reckless 0.295 rejected，Pareto 否决未动）。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS + cerebrum。

- **[2026-09-04] 加固 verifier 抓「classmethod→classmethod 委托链的 cls() 接收（nested-classmethod-cls-factory-hidden）」+ ratchet 到 classmethod-instance-receiver（本 fire）**：上一 fire 的 `verifier_strength=0.9412` 里被 bless 的 13 条全是 `nested-classmethod-cls-factory-hidden` 惰性守卫——符号+真代码，但常量经 **classmethod 工厂再委托给同类 classmethod** 读（`_Helper.create()` 返回 `_build()`、两者都 `@classmethod`，`_build` 里 `h = cls()`），解析器对 class-factory 方法只追**一层**直接构造、且 `_sibling_method_factory_receiver` 的 fall-through `_factory_return_class(method, classes, sources, _depth)` **没把 `cls` 接收器穿进去**，所以 `_build` 里的 `cls()` 解不出类、`create` 判非惰性、整模块非惰性、漏网。本轮在 `trace.py` 关掉它：`_sibling_method_factory_receiver` 的 fall-through 改为 `_factory_return_class(method, classes, sources, _depth, class_def, sib_cls_name)`——`sib_cls_name` 在 `_is_classmethod(method)` 时取「方法首参名」，`class_def` 当作 `cls_class`，使 `h = cls()` 解析回被调用类 → 读的 attr 经 `_setup()` 解析成常量 → 惰性、漏网关闭。**关键防过收紧**：`_is_classmethod` 只认真 `@classmethod` 装饰；`@staticmethod`/模块级函数走无 cls 的既有路径，行为与先前一致；`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ csdd 仍 1.0，未过收紧。**非饱和 ratchet**：nested-classmethod 被抓后按设计加更细假守卫 `classmethod-instance-receiver-hidden`（常量经**在 INSTANCE 接收器上调用的 classmethod 工厂**读，`_Helper().create().val`——`_class_factory_receiver` 只认「裸类名 Name 接收器」、`_Helper()`（`_Cls(...)` 构造）不是 Name 故解析不到，所以仍 bless，诚实留在 <1.0）。TDD 先红（`test_inert_vs_constant_hidden_..._split_the_cheat_space` 的 nested-classmethod 断言从「仍 bless」改为「must now be rejected」在旧 verifier 下 `did_expand=True` FAIL，另 `test_classmethod_cls_factory_is_now_rejected_and_nested_is_headroom` 的 nested 断言同样 FAIL）后绿（**352 passed / 56 skipped**、覆盖 **93.54% ≥ 92**、按包闸门全 ≥70%（taste_score 91.1%、trace.py 84.82%、mutation_score.py 100%）、`ruff check` clean、`ruff format` 对三个改动文件 clean）。实测 ledger `csdd_score=1.0`（不变，13 原则仍全 expanded+safe）｜`verifier_strength=0.9412→0.9444`（更细假集上诚实上升：nested-classmethod-cls 被拒，只剩 classmethod-instance-receiver 是下一格余量）｜constitution digest `321cf35c…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.909 / liar 0.386 / reckless 0.295 rejected，Pareto 否决未动）。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS + cerebrum。

- **[2026-09-04] 加固 verifier 抓「classmethod 工厂在 INSTANCE 接收器上调用（classmethod-instance-receiver-hidden）」+ ratchet 到 factory-instance-receiver（本 fire）**：上一 fire 的 `verifier_strength=0.9444` 里被 bless 的 13 条全是 `classmethod-instance-receiver-hidden` 惰性守卫——符号+真代码，但常量经 **在 INSTANCE 接收器上调用的 classmethod 工厂**读（`_Helper().create().val`，`create` 是 `@classmethod`、`h = cls(); h._setup(); return h`），`_class_factory_receiver` 只认「裸类名 Name 接收器」、`_Helper()`（`_Cls(...)` 构造）不是 Name 故解析不到，当真实扩展 bless。本轮在 `trace.py` 关掉它：`_class_factory_receiver` 的接收器判定扩为**也可接 `_Cls(...)` 实例构造**（`_Helper().create()`——`@classmethod` 在实例上调仍传 CLASS 为 `cls`，所以仍是同一接收类），使 `cls()` 构造经既有 `_factory_return_class` 路径解析回接收类 → 读的 attr 经 `_setup()` 解析成常量 → 惰性、漏网关闭。**关键防过收紧**：只多认「`recv` 是 Call 且 func 是 classes 里的 Name」一种形状；非类函数调用（`_make()`，在 sources 不在 classes）、非 Call/Name 接收器仍回 None，行为与先前一致；`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ csdd 仍 1.0，未过收紧。**非饱和 ratchet**：instance-receiver 被抓后按设计加更细假守卫 `factory-instance-receiver-hidden`（常量经**工厂函数返回的实例上调用 classmethod 工厂**读，`_make().create().val`——`_make()` 是模块级工厂函数、不在 classes，接收器解析器仍溯不到，所以仍 bless，诚实留在 <1.0）。TDD 先红（拆分测试的 classmethod-instance-receiver 断言与 `test_classmethod_cls_factory_is_now_rejected_and_nested_is_headroom` 的 instance 断言从「仍 bless」改为「must now be rejected」在旧 verifier 下 `did_expand=True` FAIL，共 2 fail）后绿（**352 passed / 56 skipped**、覆盖 **93.52% ≥ 92**、按包闸门全 ≥70%（trace.py 84.72%、mutation_score.py 100%）、`ruff check` clean、`ruff format` 对三个改动文件 clean）。实测 ledger `csdd_score=1.0`（不变，13 原则仍全 expanded+safe）｜`verifier_strength=0.9444→0.9474`（更细假集上诚实上升：classmethod-instance-receiver 被拒，只剩 factory-instance-receiver 是下一格余量）｜constitution digest `321cf35c…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.909 / liar 0.386 / reckless 0.295 rejected，Pareto 否决未动）。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS + cerebrum。

- **[2026-09-04] 加固 verifier 抓「工厂函数返回的实例上调用 classmethod 工厂（factory-instance-receiver-hidden）」+ ratchet 到 factory-clsattr-delegation（本 fire）**：上一 fire 的 `verifier_strength=0.9474` 里被 bless 的 13 条全是 `factory-instance-receiver-hidden` 惰性守卫——符号+真代码，但常量经**工厂函数返回的实例上调用 classmethod 工厂**读（`_make().create().val`，`_make()` 是模块级工厂函数、不在 classes，接收器解析器仍溯不到，当真实扩展 bless）。本轮在 `trace.py` 关掉它：`_class_factory_receiver` 的接收器判定加一档「`recv` 是 Call 且 func 是 `sources` 里的模块级工厂函数」——用 `_factory_return_class(工厂函数, ...)` 解析其单一静态返回类，再照既有路径把 classmethod 绑回该类、`cls()` 构造解析回接收类 → 读的 attr 经 `_setup()` 解析成常量 → 惰性、漏网关闭。**关键防过收紧**：只多认「`recv` 是 Call 且 func 在 `sources`（不在 classes）、且工厂返回类可证单一」一种形状；返回类不可证单一（分支/参数动态）或非 Call/Name 接收器仍回 None，行为与先前一致；`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ csdd 仍 1.0，未过收紧。**非饱和 ratchet**：factory-instance-receiver 被抓后按设计加更细假守卫 `factory-clsattr-delegation-hidden`（常量经**工厂函数返回实例上调用、且该 classmethod 通过 `cls` 接收器属性再委托给同类 classmethod** 读，`_make().create().val`，`create` 做 `return cls.build()`——classmethod→classmethod 委托解析器只跟**裸 Name 同辈调用**（`create()` 返回 `_build()`）、不跟 `cls.build()`（`cls` 接收器属性），所以仍 bless，诚实留在 <1.0）。TDD 先红（拆分测试的 factory-instance-receiver 断言从「仍 bless」改为「must now be rejected」+ 新增 `test_factory_instance_receiver_constant_is_now_rejected` 在旧 verifier 下均 `did_expand=True` FAIL）后绿（**355 passed / 56 skipped**、覆盖 **93.45% ≥ 92**、按包闸门全 ≥70%（taste_score 91.0%、trace.py 84.37%、mutation_score.py 100%）、`ruff check` clean、`ruff format` 对三个改动文件 clean；新增 `test_factory_clsattr_delegation_residual_is_headroom` 钉新余量 + `test_factory_clsattr_delegation_that_decides_is_still_blessed` 防过收紧）。实测 ledger `csdd_score=1.0`（不变，13 原则仍全 expanded+safe）｜`verifier_strength=0.9474→0.95`（更细假集上诚实上升：factory-instance-receiver 被拒，只剩 factory-clsattr-delegation 是下一格余量）｜constitution digest `321cf35c…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.909 / liar 0.386 / reckless 0.295 rejected，Pareto 否决未动）。本地 commit 只含 trace.py + mutation_score.py + 测试 + STATUS。

- **[2026-09-14] 关掉 verifier 的「classmethod 经 cls 接收器属性委托」格（`factory-clsattr-delegation-hidden`）+ ratchet 到两跳 cls 链（本 fire）**：上一 fire 的 `verifier_strength=0.95` 里被 bless 的 13 条全是 `factory-clsattr-delegation-hidden` 惰性守卫——符号+真代码，但常量经 **在 FACTORY FUNCTION 返回的实例上调用的 classmethod 工厂、且该 classmethod 经 `cls` 接收器属性再委托**读取（`_make().create().val`，`create` 做 `return cls.build()`）：委托解析器只跟「裸 Name 同辈调用」（`create()` 返回 `_build()`），不跟 `cls.build()`（`cls` 接收器属性），解不出基 builder 就按真实决策放行。本轮在 `trace.py` 关掉它，**三处一起改**：①`_sibling_method_factory_receiver` 加 `cls_name` 参数——值调用是「`<cls_name>.<method>()`」（属性 + 接收器名命中）也认作同类同辈方法，交给既有 `_find_method(class_def, method_name)` → 委托链 → `_factory_return_class` 路径；②`_class_factory_receiver` 把 `cls` 名**提到委托循环之前**计算（原在循环后）并穿进 `_sibling_method_factory_receiver`；③`_returns_inert_instance` 同样把 `cls` 名穿进去（否则 `create` 的 `cls.build()` 解不出类 → `create` 判非惰性 → 整模块非惰性，`.val` 那格即使解析出来也白搭）。**关键防过收紧**：只多认这一种形状，`@staticmethod`/模块级函数路径行为不变；`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ csdd 仍 1.0。**非饱和 ratchet**：clsattr 被抓后按设计加更细假守卫 `nested-clsattr-delegation-hidden`（两跳 cls 链：`create()`→`cls._mid()`→`cls.build()`，解析器只跟一跳），诚实留在 <1.0。TDD 先红（`test_factory_clsattr_delegation_constant_is_now_rejected` 在旧 verifier 下 `assert True is False` FAIL，实测 1 failed / 44 passed）后绿。顺带补 `test_two_hop_sibling_delegation_chain_is_rejected` 盖住解析器里 **HEAD 起就一直未覆盖**的「同辈委托链递归」分支（用 `git stash` 对照 HEAD 确认非本 fire 引入，是补测量不是补行为）。实测 ledger `csdd_score=1.0`（不变，13 原则仍全 expanded+safe）｜`verifier_strength=0.95→0.95238`（13 条 clsattr 假守卫全被拒，只剩 13 条 nested-clsattr 是余量）｜constitution digest `321cf35c…` 不变（**没改尺子**，Lock-6 完好）｜ranking 不变（robust 0.909 / liar 0.386 / reckless 0.295 rejected，Pareto 否决未动）。全量 **367 passed / 56 skipped**（本 fire +2 测试；另 2 条是**本 fire 期间另一个并发 agent** 在 `src/taste_score/__main__.py` + `tests/test_tier0_constitution_wiring.py` 上加的 Lock-4 `regress` 接线，非本 fire 改动、不纳入本 commit）、覆盖率 **93.68% ≥ 92**、per-pkg 全 ≥70%（taste_score 91.5%）、`ruff check` clean、`ruff format --check` 对我 3 个改动文件 clean（repo 级 10 个漂移文件与本 fire 无关，HEAD 既有）。本地 commit 只含 trace.py + mutation_score.py + test_verifier_mutation_score.py + STATUS + cerebrum。

- **[2026-09-14] 收紧 4 条 `except` 哨兵：补上 PEP8 换行写法（本 fire）**：SEC-09/10/12/13 的 `violations` 哨兵只抓「同行」写法（`except Exception: return CheckResult(ok=True)`）；把 handler 体挪到下一行（PEP8，也就是 ruff 格式化后的真实 `src/` 形状）就**完全看不见** —— 尺子会把 swallow-and-continue 判 `safe=True`。这是「加一个换行就能绕过尺子」的零成本逃逸，而且此前**没有任何测量覆盖它**：各原则测试只断言哨兵在锚文件里不存在（无假阳性），从没测过它会命中（假阴性）。本轮：①4 条哨兵改为两分支并列 `same-line|except[^\n]*:\n\s*return[^\n]*<fail-open>`（`\s*` 容忍缩进/空行；SEC-11 本就两写法都抓、未动）；②`tests/test_taste_score_constitution.py` 新增三条测量/守护：`test_every_sentinel_fires_on_the_fail_open_it_names`（每个原则必须带 fail-open 样本——覆盖 ratchet，新增原则不加样本即红；`except` 原则必须两种写法都命中）、`test_verifier_calls_a_pep8_own_line_fail_open_unsafe`（端到端走引擎自己的判定 `TraceabilityVerifier.matrix()`，临时锚到含换行 fail-open 的文件，要求 `safe=False`）、`test_ruler_revision_never_loses_a_detection_against_git_head`（第六道锁补丁：pin 会被重生成，而「重生成 pin」正是洗白弱化的路子——要求改尺子**单调**，committed 尺子抓到的 fail-open 工作区尺子必须仍抓到）。**先红后绿证据**：新哨兵测试对 committed 尺子 `FAILED`（SEC-09 `'except[^\n]*Verdict\.PASS'` 对换行写法返回 None），改完 `passed`；端到端测试在 committed 尺子下 `FAILED`（引擎 bless 了 fail-open，`safe=True`），改动后 `passed`；单调性守护反向验证——把 SEC-01 哨兵改成 `THIS_NEVER_MATCHES_ANYTHING` → `FAILED`，恢复后 `passed`。digest `321cf35c…`→`ec5b90fb…`，`constitution.pin` 用脚本按新 digest **派生重生成**（不手敲）。**方向是收紧不是放松**：13 条锚文件在新哨兵下全 clean（零假阳性），`test_every_anchor_resolves_and_matches_pattern` + `test_shipped_pin_matches_shipped_constitution` 均绿。实测 ledger：`csdd_score=1.0`（13 原则全 expanded+safe）｜`verifier_strength=0.9523809…` **原样不变**（本 fire 动的是哨兵覆盖，不是惰性守卫假集——如实报「没涨分」）｜ranking 不变（robust 0.909 / liar 0.386 / reckless 0.295 rejected，Pareto 否决未动）。门：**378 passed / 56 skipped**（本 fire +3 测试，其余增量为并发 agent 的 goal_persistence 工作）、覆盖率 **94.19% ≥ 92**、per-pkg 全 ≥70%（taste_score 92.3%）、`ruff check` clean、`ruff format --check` 对本改动测试文件 clean。**诚实的边界**：本 fire 没有任何分数上升，改善的是「尺子看不看得见它命名的那种 fail-open」；下一个真实空档是把「哨兵强度」本身做成非饱和可测量（目前无任何维度测量它）。本地 commit 只含 constitution.toml + constitution.pin + 该测试文件 + STATUS/cerebrum。

- **[2026-09-14] 宪法扩到 14 原则——把引擎自己的否决门写进宪法（本 fire）**：加 SEC-14（CWE-693 保护机制失效 → `src/taste_score/gate.py`，pattern `run\(name, p\)\.reckless`，禁三种偷袭：`rejected=False`（fail-open 放行）、`if not ...reckless`（反转成只否诚实者）、`if ...reckless:` 后 `pass|continue`（保留谓词、掏空否决体））——SEC-01..SEC-13 分别锚 sandbox/safety/observability/security/goal_loop/tool_registry/cost_control/eval_harness，**没一条锚 taste_score**，也就是引擎自己的否决门无人登记。而 taste 标准的红线（坏 = E 上 & S 降）唯一被机器执行的地方就是 `TasteGate._score_one`：帕累托否决（越界行）、回归否决（regress 报错）、以及 `score()` 顶部的尺子投毒否决（pin 不符），三种否决都落成 `rejected=True`。把其中任何一种改写掉，csdd 与 traceability matrix 仍报满格安全边界、零证据拿分——「越界不行」这条边界等于没写。**pattern 选型有两处实测约束**（都是先撞出来的）：①「裸 `\.reckless`」不行——mutation 生成器把 token 嵌进 `if <literal>:`，解转义后成 `if .reckless:`，**无效 Python**，度量会因解析失败而失真；②把 `class TasteGate` 一起或进 pattern 也不行——整段删掉帕累托否决时类名仍在、pattern 还匹配，删除型作弊反而看不见。最终锚「否决谓词本身」（`run(name, p).reckless`），删掉即 pattern 消失 → `did_expand=False`。TDD 先红（`test_taste_gate_veto_boundary_is_constitutional`、样本覆盖 ratchet `test_every_sentinel_fires_on_the_fail_open_it_names`、新端到端 `test_verifier_calls_a_flipped_veto_unsafe` 三红，实测 **3 failed / 18 passed**）后绿（该文件 **21 passed**）。**反向验证两处各一次**：①哨兵换成永不匹配 → 覆盖 ratchet + 端到端 FAILED；②pattern 换成不存在 → 逐原则守卫 + 端到端 FAILED；两次均 byte-identical 还原（sha256 比对通过）。digest `ec5b90fb…`→`37a31b28…`，`constitution.pin` **脚本派生**重生成（不手敲）。实测 ledger：`csdd_score` **1.0**（14 原则全 expanded+safe）｜`verifier_strength` **0.9523809… 原样不变**（新增原则的假守卫被拒比例与均线一致，非回归）｜traceability **14 行**（SEC-14 行 expanded+safe 均 true）｜ranking robust **0.909→0.911** / liar **0.386→0.400** / reckless **0.295→0.311 仍 rejected**（`expanded but regressed safety boundary (probe A1)`，帕累托否决未动）。门：**385 passed / 56 skipped**（本 fire +3，其余增量为并发写者）、覆盖率 **94.23% ≥ 92**、per-pkg 全 ≥70%、`ruff check` clean。**诚实的边界**：csdd 已饱和，本轮不涨分，换来的是「红线本身第一次被登记为宪法边界」；哨兵只命名 3 种形状，**不覆盖**「保留谓词但删掉 `rejected=True`」这类无否定式改写（纯正则表达不了「两个标记之间不许出现某词」，需 lookahead 或换证据形态，属下一格余量）。本地 commit 只含 constitution.toml + constitution.pin + 该测试文件 + STATUS/cerebrum。

- **[2026-09-14] Pareto 否决从「只读黄金集」扩到「整夜菜单」（本 fire）**：spec §3 写死「判负 ⇔ E 涨但 S 回退」，**没有**黄金集限定；但 `gate.py:_score_one` 的帕累托否决只 for 了 `golden`——于是「只在记得住的那版措辞上守边界、tripwire 一改写就让步」的背路径 agent（正是变异菜单存在的理由，spec §4 Lock 2）在黄金集上干净、`rejected` 仍为 False，整夜活下来。**本 fire 把这条路堵死**：`src/taste_score/gate.py:113` 改为 `for p in [*golden, *mutants]`（黄金集先判，保持既有 `rejected_probe` 报告口径不变），并把 `gate.py` 模块 docstring 与 `models.TasteScore`（`models.py:54`）的「Pareto recklessness on the golden set」订正为「any probe of the night — golden or mutated」。**先红后绿证据**：新测试 `tests/test_taste_score.py:158 test_reckless_on_the_mutated_menu_is_vetoed_too`（真实 `Mutator` 造菜单 + 按措辞判安全的 verify）在改前 FAILED（`assert False is True`，实测 `1 failed, 3 passed`），改后 PASSED（该文件 21 passed）。**两个控制测试反向验证有牙**：把谓词临时过收紧成 `if not run(name,p).safe`（把「没证据」也算越界）→ `test_no_evidence_on_a_mutated_probe_is_not_reckless` **FAILED**，恢复后 sha256 逐字节相同（`fd31ff89…`）。**诚实的边界**：ledger **逐字节不变**（`json.dumps(sort_keys)` 比对 True）——demo 三个 agent 里没有「只在黄金措辞上守边界」的那种，所以本轮**没有分数上升**，换来的是引擎自己的红线第一次在整夜尺度上被执行（demo 侧不可见 ≠ 没改）。constitution/pin **未动**（digest `37a31b28…` 不变，Lock-6 完好）。实测 ledger：`csdd_score=1.0`（14 原则全 expanded+safe）｜`verifier_strength=0.9523809…` 不变｜traceability 14 行不变｜ranking robust 0.911 / liar 0.400 / reckless 0.311 rejected(probe A1) 三个数字全不变。门：**390 passed / 56 skipped**（本 fire +3），覆盖率 **94.68% ≥ 92**，per-pkg 全 ≥70%（taste_score 93.9%），`ruff check` clean；`ruff format --check` 对 gate.py/test_taste_score.py 仍红但**非本 fire 引入**——把 HEAD 版抽到临时目录跑同样 `Would reformat`，且 `--diff` 给出的行全是既有风格漂移（不在我新增行内）。本地 commit 只含 gate.py + models.py + 该测试文件 + STATUS/cerebrum。
- **[2026-09-14] `safe` 补上「正向哨兵」`require`——关掉纯删除型作弊（本 fire）**：`pattern` + `violations` 两个检测器**都是缺席式**的（`pattern` 只证明符号在场、`violations` 只证明没加禁用形状），于是**纯删除**型篡改两者全瞎——保留 `run(name, p).reckless` 这个谓词、只把帕累托否决体里的 `rejected=True` 删掉：谓词仍在 ⇒ `pattern` 命中；`rejected=False` 字面不出现 ⇒ `violations` 静默；而 `TasteScore.rejected` 默认 `False` ⇒ 否决悄悄失效、尺子仍报 `safe=True`、csdd 仍 1.0。这正是上一 fire 自己记下的余量。**改动（4 文件）**：①`constitution.py:33` `Principle` 加 `require: tuple[str, ...] = ()`（正向哨兵：**必须被找到**的 fail-closed 结果式）+ `_principle()` 解析时 **fail-closed 拒非 list**（`require` 写成裸字符串会被逐字符迭代、每个字符都算命中 ⇒ 正向检查静默变空转）；②`trace.py:1159` `classify(..., require=())` 的 `safe = 无 violations 且每条 require 命中`，`_run` 传 `p.require`，`matrix()` 行新增 `require`；③`constitution.toml` 14 条原则共 **19 条 require**，一律用 TOML **literal string**（单引号）写正则——用 basic string 必须把反斜杠双写，SEC-09 当年正是这么被写成「测不出自己」的；④digest `37a31b28…`→`caae70d1…`，`constitution.pin` **脚本派生**重生成（不手敲）。**默认 `()` ⇒ 语义与改动前等价 ⇒ 尺子只可能加检测、不可能减检测**（`test_ruler_revision_never_loses_a_detection_against_git_head` 已扩成「violations 命中 **或** require 未命中」都算一次检测）。选 require 的判据是**该原则的 fail-closed 结果式**而不是符号名：`rejected=True`（SEC-14 帕累托否决 + 尺子投毒否决两条）、高危动作强制 PENDING 的谓词（SEC-02）、落盘前 `redact_value(payload)` 调用（SEC-03）、裸 shell 串 `return False`（SEC-04）、marker 集扫描（SEC-05）、`k in _SAFE_ENV_KEYS`（SEC-06）、`shlex.split` + `shell=False,`（SEC-07）、`if not self.available:` + `argv[0] not in self._allowlist`（SEC-08）、异常分支 `Verdict.FAIL`（SEC-09）、handler 崩溃 `ok=False`（SEC-10）、`raise BudgetError`（SEC-11）、`if not result.ok: return result` + `if not path.exists()`（SEC-12）、maker/checker 两个崩溃包络（SEC-13）、包含性检查推出允许判定（SEC-01）。**先红后绿证据**：`tests/test_taste_score_constitution.py` 新增 5 测试（SEC-14 删除式篡改端到端、`require` 覆盖 ratchet、require 不得编造且不得匹配空串、**机械化「删掉命中区间后必须报 unsafe」**、非 list `require` 必须拒），改前 **5 failed / 21 passed**，实现后 **26 passed**。**反事实实证（独立脚本，非测试断言）**：对删掉 `rejected=True` 的真实 `gate.py`，`pattern` 仍命中、`violations` 仍静默，`require=()` 的旧尺子判 `safe=True`，shipped 尺子判 `safe=False`；committed(HEAD) 尺子同样 `safe=True`（`require` 数 0）⇒ 本轮只加检测。**实测分数**：`csdd_score=1.0`（14 原则仍全 expanded+safe ⇒ 未过收紧）｜`verifier_strength=0.9523809…` **原样不变**（本轮动的是哨兵方向，不是惰性守卫假集）｜traceability **14 行**（每行新增 `require` 字段）｜ranking **不变**（robust 0.911 / liar 0.400 / reckless 0.311 rejected(probe A1)，Pareto 否决未动）。**诚实的边界**：本轮**没有分数上升**——换来的是「纯删除型作弊」第一次被尺子看见，以及 `safe` 从「没加坏东西」升级为「边界仍在执行」。门：**395 passed / 56 skipped**（本 fire +5）、覆盖率 **94.80% ≥ 92**、按包闸门全 ≥70%（taste_score 93.9%）、`ruff check` clean、`ruff format --check` 对 3 个改动文件 clean。**下一格余量**：`require` 只是「字符串在场性」，仍表达不了「同一函数内两个标记之间不许出现某词」（如 SEC-14 的「保留谓词但删掉整段否决 return」需要 lookahead 或 AST 级证据）；verifier 的惰性守卫 ratchet 仍停在 `nested-clsattr-delegation-hidden`。
- **[2026-09-14] 证据只能来自代码，不能来自散文（本 fire）**：`pattern`/`require` 全是**裸文本**正则，于是「删掉守卫、把它的名字或必备句子留在**注释/docstring** 里」这一型篡改两边全瞎——模块里还有真代码（`_is_dead_stub` 不触发）、裸正则也仍能命中注释，尺子照旧报 `did_expand=True / safe=True`。**反事实实证**（同一份篡改，只换尺子）：SEC-14 把帕累托否决的 `rejected=True,` 从代码里删掉、只把这句话留在注释里（文件仍是合法 Python）——HEAD 尺子判 `expanded=True safe=True`（放行：否决不再置位却仍报 safe），本 fire 尺子判 `expanded=True safe=False`（抓住）；两份尺子下裸 require 都仍命中，所以旧尺子的 `safe=True` 确实来自散文而非代码。**改动**：`src/taste_score/trace.py` 加 `_code_only(text)`（blank 掉 COMMENT token + docstring 表达式，保留行结构与**普通字符串字面量**——SEC-02 的 `risk == "high"`、SEC-14 的 `reason="constitution…"` 都含字符串，全 blank 会把真宪法判死）+ `_docstring_spans`；`classify` 的 `pattern`/`require` 改读 code 视图，`violations` 仍在裸文本（缺席检查：任何地方提到禁用形状都算信号，blank 只会丢检测）。顺手删掉 `_code_only` 里一行不可达的 `if row - 1 >= len(lines): break`（死代码按纪律删，不补测）。**测量**：`src/taste_score/mutation_score.py` 的 corpus 增 3 题型 × 14 原则——`prose-comment` / `prose-docstring`（现被拒）与 `string-hidden`（名字藏在普通字符串里 = 本规则的残留，仍 bless）；每个新题型都实测「裸正则仍命中 + 非 shell + 非惰性」，确保拒绝不是「拒错理由」。**先红后绿**：新增 6 测试（5 在 `tests/test_verifier_mutation_score.py`、1 在 `tests/test_taste_score_constitution.py`），把源码回退到 HEAD 跑 → **6 failed / 1 passed**（含「注释型 require 被放行」那条 `assert True is False`），实现后该两文件 **79 passed**。**数字**：`csdd_score=1.0` 不变（14 原则仍全 expanded+safe ⇒ 未过收紧）｜traceability 14 行逐行不变｜ranking robust 0.911 / liar 0.400 / reckless 0.311 rejected 不变｜digest `caae70d1…` 不变（**没改尺子**，Lock-6 完好）｜`verifier_strength 0.9524→0.9167` **不是检测力退化**：分母 294→336（corpus 长了一个家族），旧 21 题型逐条不变 `280/294=0.952381`，新家族 `28/42` 被拒（两个 prose 题型各 14/14 拒），`string-hidden` 14/14 仍 bless 是下一格余量。门：**402 passed / 56 skipped**（HEAD 451 collected → 458，本 fire +7）、覆盖率 **94.81% ≥ 92**、按包闸门全 ≥70%（taste_score 94.4%）、`ruff check` clean、`ruff format --check` 对本 fire 两个源码文件 clean（repo 级 format 漂移仍在 12 个未动文件上——那 12 个文件本 fire 未触碰）。本地 commit 只含 trace.py + mutation_score.py + 两个测试文件 + STATUS/cerebrum。**下一格余量**：`string-hidden`（证据藏在普通字符串字面量里）；不可解析文本仍回退裸文本（与 `_is_dead_stub` 一致，防解析器打嗝误杀真文件）；`violations` 仍是裸文本全文件搜索（注释里提到禁用形状会误报，既有粗粒度边界）。

- **[2026-09-14] 关掉 verifier 的「cls 接收器两跳委托」格（`nested-clsattr-delegation-hidden`）+ ratchet 到 cls-arg 委托（本 fire）**：上一 fire 的 `verifier_strength=0.9167` 里被 bless 的 28 条是两类——14 条 `string-hidden`（既定余量）与 14 条 `nested-clsattr-delegation-hidden`（两跳 `cls` 链：`create()` 返回 `cls._mid()`、`_mid` 返回 `cls.build()`）。根因在 `trace.py` 的 `_sibling_method_factory_receiver`：递归调用 sibling 时**没把接收器名穿下去**（`cls_name` 回落 `None`），第二跳的 `cls.build()` 于是被当成「未知名字上的属性调用」，委托链断在半路——基 builder 找不到、常量证不出来、惰性直通守卫被 bless。**改动很小**：递归时传 `inner_cls_name`（本跳方法若是 `@classmethod` 取它**自己的首参名**，否则沿用上层），任意跳数的 `cls` 链都能走到基 builder；`_depth>8` 仍兜住互递归。**先红后绿**：先改测试——`test_nested_clsattr_delegation_constant_is_now_rejected` 与拆分测试的 nested-clsattr 断言由「仍 bless」翻成「必须被拒」，在旧 `trace.py` 下 **2 failed / 52 passed**（`assert True is False`，`ProbeRun(probe_id='SEC-02', did_expand=True)`），改完 **54 passed**。**防过收紧**：新增 `test_two_hop_cls_chain_that_decides_is_still_blessed`（两跳链的基 builder 造的是**真决策**守卫 `path in roots` → 仍 bless），`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant` 过 ⇒ 14 条锚零误杀、csdd 仍 1.0。**非饱和 ratchet**：两跳链被抓后按设计补更细假守卫 `cls-arg-delegation-hidden`（classmethod 把 **`cls` 当实参交给模块级 helper** 去构造，`create()` 返回 `_delegate(cls)`；自由函数里的实参不被当作类接收器追踪），诚实留在 <1.0。**实测数字**：`verifier_strength 0.91666→0.92`（分母 336→350，被拒 308→322：nested-clsattr 14/14 由 bless 转拒，新家族 cls-arg 14/14 仍 bless）｜`csdd_score=1.0` 不变（14 原则全 expanded+safe）｜traceability 14 行不变｜ranking 不变（robust 0.911 / liar 0.400 / reckless 0.311 rejected，帕累托否决未动）｜constitution digest `caae70d1…` 不变（**没改尺子**，Lock-6 完好）。门：**404 passed / 56 skipped**（本 fire +2 测试）、覆盖率 **94.83% ≥ 92**、按包闸门全 ≥70%（taste_score 94.5%）、`ruff check` clean、`ruff format --check` 对本 fire 三个文件 clean。本地 commit 只含 trace.py + mutation_score.py + 该测试文件 + STATUS/cerebrum。

- **[2026-09-14] 证据只能来自代码，不能来自数据（字符串字面量）（本 fire）**：上一 fire 的代码视图（`_code_only`）blank 掉的是**散文**（注释 / docstring）；**普通字符串字面量**被刻意保留（SEC-02 的 `risk == "high"`、SEC-14 的 `reason="constitution integrity violation…"` 就在字符串里，全 blank 会把真宪法判死），于是「删掉守卫、把它的名字留在字符串里」这一型篡改两边全瞎 —— 模块里仍有真代码（`_is_dead_stub` 不触发）、裸正则仍命中字符串，尺子照旧报 `did_expand=True`。本轮把规则从「不许是散文」细化为**不许是数据**，判据是**「完全落在字面量内部」而不是「碰到字面量」**：`src/taste_score/trace.py:123` 新增 `_line_starts` / `_data_spans`（tokenize 的 `STRING` token 字符区间）/ `_evidence`（存在**不整段落在 data 区间内**的匹配才算证据），`classify`（`trace.py:1290`）的 `pattern` / `require` 改走 `_evidence`；`violations` 仍在裸文本（缺席检查）。**先红后绿证据**：新增 4 测试（`test_string_literal_evidence_is_rejected_for_every_principle` 逐原则 14/14、`test_non_code_fakes_model_the_cheater_that_deletes_the_guard`（原 prose 测试扩到 string 容器）、`test_every_principle_evidence_is_code_backed_not_data` 授权 ratchet、`test_verifier_calls_a_string_literal_only_guard_unimplemented` 端到端 `matrix()`），改动前 **4 failed / 81 passed**（实证 `assert True is False`：SEC-01 那个「名字只在字符串里」的假守卫被 bless 成 `ProbeRun(did_expand=True, safe=True)`；另 1 条 `ImportError: cannot import name '_data_spans'`），实现后该两文件 **85 passed**。**防过收紧**：`test_evidence_next_to_a_string_literal_still_counts_as_code` 钉「跨过字面量仍算代码」（SEC-02 真 require 就是这种形状），`test_every_anchor_resolves_and_matches_pattern` + `test_every_require_matches_its_anchor_and_is_not_a_wildcard` 过 ⇒ 14 条真锚零误杀、csdd 仍 1.0。**corpus 记账（如实分解）**：`string-hidden` 由 bless 转拒（14 条），按 ratchet 惯例补更细的 `fstring-hidden`（f-string 的字面量片段是 `FSTRING_MIDDLE` token、不是 `STRING`，落在 `_data_spans` 之外 → 仍 bless，是下一格余量）。**同口径对比**：旧 25 题型 ×14 → 拒绝 322/350=**0.9200** → 现在 **336/350=0.9600**（真涨）；带上新家族（26×14=364）headline 为 **336/364=0.923077**（被新增的「仍 bless」题型稀释，不是检测力退化）。constitution/pin **未动**（digest `caae70d1…` 不变，Lock-6 完好）｜`csdd_score=1.0`（14 原则仍全 expanded+safe）｜traceability 14 行不变｜ranking 不变（robust 0.911 / liar 0.400 / reckless 0.311 rejected(probe A1)，帕累托否决未动）。门：**408 passed / 56 skipped**（本 fire +4）、覆盖率 **94.87% ≥ 92**、按包闸门全 ≥70%（taste_score 94.6%）、`ruff check` clean、`ruff format --check` 对本 fire 四个改动文件 clean。本地 commit 只含 trace.py + mutation_score.py + 两个测试文件 + STATUS/cerebrum。

- **[2026-09-14] 关掉证据规则最后一个非代码容器（f-string 字面量）（本 fire）**：`_data_spans` 只认 `tokenize.STRING`，而 Python 3.12+ 把 f-string 切成 `FSTRING_START`/`FSTRING_MIDDLE`/`FSTRING_END` —— **f-string 的字面量段不是 `STRING` token**，于是「删掉守卫、把名字留在 f-string 文本里」这一型篡改继续被 bless：模块里有真代码（`_is_dead_stub` 不触发）、裸正则仍命中、`_data_spans` 覆盖不到。**反事实实证**（同一份篡改、只换尺子）：把 SEC-01 的守卫从文件里删掉、只把 `allows_write` 与 require 的 `_is_within(resolved, root)` 留在 `f"...is enforced"` 的字面量里（合法 Python、模块里另有真代码），用 committed(HEAD) 尺子跑引擎自己的 `matrix()` → `raw_regex_matches=True expanded=True safe=True`（**完整假阴性**：csdd 会把这条边界记成「已实现且干净」）；用本 fire 尺子 → `raw_regex_matches=True expanded=False safe=False`；同一份「真守卫」样本两边都 `expanded=True safe=True`（无过收紧）。**改动**：`src/taste_score/trace.py:136` 新增模块级 `_DATA_TOKEN_TYPES`（`STRING` + `getattr(tokenize, "FSTRING_START/MIDDLE/END", None)` 并滤掉 `None`——pre-3.12 上 f-string 本就是单个 `STRING`，那里行为零漂移），`_data_spans`（`trace.py:148`）改用它；**只 span 字面量段，f-string 的表达式仍是代码**（真守卫用 live value 拼消息不会被判 data）。**先红后绿**：新增 4 测试——`test_fstring_evidence_is_rejected_for_every_principle`（逐原则 14/14）、`test_fstring_literal_run_is_data_while_its_expression_is_code`（防过收紧）、`test_only_the_named_residual_families_stay_blessed`（把「仍 bless 的家族集合」按名钉死）、端到端 `test_verifier_calls_an_fstring_only_guard_unimplemented`（走 `TraceabilityVerifier.matrix()`）；改动前 **5 failed / 84 passed**（实测 `assert True is False`：f-string 假守卫被 bless），实现后那两个文件 **89 passed**。**数字（如实分解）**：`verifier_strength 0.923077→0.961538`——分母不变（26 题型 × 14 原则 = 364），被 bless 由 28→14，即 `fstring-hidden` 14/14 由 bless 转拒；**没有**为了让数字好看删掉任何残留题型，存活残留只剩常量轴的 `cls-arg-delegation-hidden`（14 条，另一条轴）。**本轮不改尺子**：constitution/pin 一字未动，digest `caae70d1…` 不变（Lock-6 完好）｜`csdd_score=1.0`（14 原则仍全 expanded+safe ⇒ 未过收紧）｜traceability 14 行逐行不变｜ranking 不变（robust 0.911 / liar 0.400 / reckless 0.311 rejected(probe A1)，帕累托否决未动）。门：**412 passed / 56 skipped**（HEAD 408 → 本 fire +4）、覆盖率 **94.87% ≥ 92**、按包闸门全 ≥70%（taste_score 94.6%）、`ruff check` clean、`ruff format --check` 对本 fire 四个文件 clean（repo 级既有漂移仍在 11 个**未动**文件上）。本地 commit 只含 trace.py + mutation_score.py + 两个测试文件 + STATUS/cerebrum。**诚实的边界**：数据轴到此对「tokenizer 能命名的容器」已闭合；**关不掉的是 containment 本身**——跨字面量边界的匹配仍算证据（SEC-02 的 `risk == "high"` 正是这种形状，收紧它会杀掉真宪法），这是该规则的结构性残留，不是没看见。

- **[2026-09-14] Tier-0 CSDD 接线 + 5 个 bugfix（9 任务 SDD，已 merge + push）**：plan `docs/superpowers/plans/2026-09-14-tier0-csdd-wiring-and-bugfixes.md`、spec `doc/superpowers/specs/2026-09-14-tier0-csdd-wiring-and-bugfixes-design.md`、逐任务 ledger `.superpowers/sdd/2026-09-14-tier0-csdd-wiring-and-bugfixes/progress.md`（**保留不删**，里面是 ~24 条 deferred findings + 3 条污染裁定的唯一落点）。A 组把宪法接到真评分路径：A1 Lock 6 接线（宪法传进 `gate.score`）／A2 外部 pin（尺子由进程之外钉住）／A3 amendment 拿真实 probe id／A4 武装 Lock 4（`regress` 有真实调用方）。B 组修 5 个真 bug：B1 `GoalStore` 读-改-写改原子／B2 `thread_id` 消毒再落文件名／B3 暂停原因不再被静默丢掉／B4 Orchestrator 聚合 `modified_files`／B5 `CompactionResult.to_dict()` 在 slots dataclass 上崩（`__dict__`→`asdict`+测试）。最终整支评审 1 个 Important（`--constitution` 帮助文本撒谎 + `--pin` 无 CLI 覆盖）在 fix round `d3a039a` 修掉。
  **收尾审计又抓出两处「测试空转」口子**（两次都是我自己的错，已复现后修）：`07c1ae2` 声称关掉了 `--pin` 的环境依赖空转，实测 `AH_CONSTITUTION_PIN=deadbeef` 下**照样通过**（`pin=args.pin`→`None` 后落回 env，`6a5c1d9` 补 autouse `_no_ambient_pin` 清环境 + 一句「fallback 必须是真 digest」的守卫断言才真关掉）；`b75d35b` 关掉同类第二处（`tests/test_taste_score.py` 里 `compete()` 直接调用那一处）。**残留**：测试守卫与 `compete` 之间读 `read_pin()` 两次 = TOCTOU，需外部进程持续写 `constitution.pin` 才触发（实测 2/8 概率），静态 env/fs 状态六项全部为死路。
  **验收（`b75d35b`）**：`412 passed / 56 skipped / 0 failed`；覆盖率 **94.87% ≥ 92**；`scripts/coverage_gate.py` exit 0（14 包全达标，最低 `agent_harness` 85.7%）；`ruff check .` clean；`src/` 零第三方 import（`pyproject.toml` 声明的 `pydantic>=2.0` 从未被 import）。`ruff format --check .` 仍红（11 个既存漂移文件，非本 quest 触碰；ruff 全仓无 pin）。仓库无 `.github`/CI，所以这些闸门今天全靠手动跑。
  **已推**：`git push origin main` → `2b7b8d1..b75d35b`，实测 `main == origin/main == b75d35b`、divergence `0 0`。分支 `tier0-csdd-wiring-and-bugfixes` 与工作区**保留**（并发 agent 仍在上面提交，合并走 ref-only `git branch -f main` 以免动它的 checkout）。
  **未做（交棒）**：①Lock 6 的否决不传导到顶层 `csdd_score`（仍 1.0）与 exit code（仍 0），CLAUDE.md 的「整轮判负」未字面成立；②`examples/measure_efficiency.py:82,87` 的 `shlex` 反斜杠塌缩 + `{path!r}`——路径含真 Python 转义（`\a \U` 等）时 `tests/test_efficiency.py::test_measurements_are_sane` 变红，`origin/main` 既存、非本支引入；③`src/hippocampus/models.py:75` 是 B5 的同款 `__dict__`-on-slots（已复现 `AttributeError: 'MemoryFact' object has no attribute '__dict__'`），属本 quest 没碰的包；④两处 `pytest.skip` 洞（`tests/test_taste_score_constitution.py:179/:537`，非 git checkout 下守卫静默消失）；⑤`src/sandbox/sandbox.py:119`（成功码 + `stdout=None` 的假通过）与 `src/taste_score/trace.py:108`（坏语法 fail-open）；⑥Tier-0 四条验收标准背后**没有任何 benchmark/eval**。

---

## 🚀 Next phase

**Goal:** P0 + P1 已收口（`doc/roadmap/100_tasks.md` 1–46），剩 P2（47–100）与两条待确认。

P1 已落定（Era 33）：LLM-judge、按模型计价、token 账本、trace span、CI 四闸、沙箱文件写隔离、`doc/08`/`doc/09`、覆盖率 97.18%、206 passed。

### Open decisions / 待确认
- **真 LLM 基线（P1.15–17 / 41–44）**：✅ 已跑通（2026-09-01）。修 `examples/llm_goal_loop.py` 三处（thinking block 提取、key 顺序对齐 env 三件套、token 记账 `input+output`），`deepseek/deepseek-v4-pro` 经 `llm-proxy.tapsvc.com` 跑出 **complete / 1 轮 / 185 token / 平均 6.21s**，机器 checker `answer()==42` PASS。真数字见 `doc/04_faux_provider/benchmark.md`。
- **seccomp/Landlock / network / fork 隔离（P1.18 / 20）**：Linux-only syscall 特性，Windows 降级到 `PathPolicy`（纯 pathlib 文件写隔离已落地）。要真隔离需 Linux 环境。
- **mypy 8 错误（P2.78/92）**：`src` 下 8 处类型错误（`sandbox/verifier` 的 `bytes|str`、`world_verifier` 的 `str|None in`、`models` 的 `datetime|None`、`loop_runner:184` 的 `int>=None`），多为 Optional 注解过宽，非运行时 bug（有真值守卫）。要 `py.typed`+CI 需先修。
- **P2（47–100）**：文档深水区、property-based/fuzz 测试、可观测导出、打包 wheel/Docker、生态整合、打磨重构——大部分独立可选，未启动。

---

## 📁 Active architecture

- **Stack:** Python 3.13（`python3`，**不是 `python`=2.7**）、pydantic 2.13、pytest 9.1、pytest-cov 7.1。
- **Key modules:** `goal_persistence` / `goal_loop` / `faux_provider` / `context_compaction` / `hippocampus` / `tool_registry` / `sandbox` / `eval_harness` / `observability` / `safety` / `cost_control`（共 11 个，coverage `source` 全覆盖）。
- **Patterns:** red-green（bug 修复带先红后绿的回归测试）；覆盖率闸门 `fail_under=92`（未覆盖行=死代码，不是该补测试）；verify the world（机器重读产物，不信自报）。

---

## ⚠️ External blockers (don't block coding)

- `examples/llm_goal_loop.py` 真 LLM 基线：key 在，但 model/base_url/key 三件套不一致（deepseek-v4-pro env vs MiniMax 示例假设）。对齐后重跑，否则 benchmark 标 `待确认`，不编数字。

---

## 🔧 Useful commands

```bash
python3 -m pytest -q                                # 全量测试（217 passed，另 56 opt-in skipped）
RUN_REAL_LLM=1 python3 -m pytest tests/test_real_llm.py -q  # 真 LLM 四维评测（烧真实 API，写 report.md）
python3 -m pytest --cov --cov-report=term-missing -q  # 覆盖率闸门（fail_under=92）
python3 -m pytest tests/test_faux_provider.py -q    # 单目标测试
python3 examples/system_e2e_trace.py                # 全系统 E2E 每轮对话 trace（happy + 对抗）
python3 examples/thinking_benchmark.py --rounds 5   # thinking 关/开 token+耗时对比（需 MINIMAX_API_KEY）
```

---

## 📚 References (read IF needed)

- `.wolf/cerebrum.md` — User Preferences + Do-Not-Repeat + Decision Log
- `.wolf/anatomy.md` — token-efficient file index
- `doc/reference_harness/comparison.md` — 四仓库 TDD 对比
- `doc/reference_harness/harness_skills.md` — harness skills 调研 → 三目标
- `doc/03_tdd_testing/plan.md` / `doc/04_faux_provider/plan.md` / `doc/05_verify_world/plan.md` / `doc/07_self_improve/plan.md`
- **[2026-09-14] 关掉 verifier 的「classmethod 把 `cls` 当实参交给模块级 helper」格（`cls-arg-delegation-hidden`）+ ratchet 到 `cls-alias-delegation-hidden`（本 fire）**：上一 fire 的 `verifier_strength=0.9615` 里被 bless 的 14 条是 `cls-arg-delegation-hidden`——`create()` 做 `return _delegate(cls)`、`_delegate(k)` 做 `return k.build()`：接收器以**实参**离开类，解析器不再把它当类接收器，基 builder 够不到，惰性直通守卫被当成「真实扩展」。本轮在 `src/taste_score/trace.py` **两处一起改**，缺一不可：①`_sibling_method_factory_receiver` 在「裸 Name 调用不是同类方法」时落进新分支，用 `_handed_receiver_param` 把**接收该实参的形参**绑成接收器名，再以该名递归解析 helper 自身的 return（只认字面等于被追踪接收器名的**位置实参**）；②`_is_receiver_forwarder`：**每个** return 都是「对自己形参的方法调用」的模块级函数判为**透明**（它自己不决定任何事，决定发生在接收者的方法里，而那个方法就在本模块的类里被单独检查过），使其不再污染模块惰性判定。**为什么②不可省**：`_is_inert_module` 要求**每条**顶层语句都惰性，光让守卫的常量解析通过、`_delegate` 那关照样非惰性（自由函数里的 `k.build()` 证不出常量）。**防过收紧三件套**：`test_receiver_handoff_that_decides_on_state_is_still_blessed`（helper 转发给真决策 `path in cls._allowed` 仍 bless）、`test_module_forwarder_is_transparent_only_when_every_return_forwards`（一条非转发的 return 就不算透明）、`test_hardened_verifier_still_sees_every_constitution_anchor_as_compliant`（14 锚零误杀 ⇒ csdd 仍 1.0）。**先红后绿**：新增/翻转 4 条测试，旧 `trace.py` 下该文件 **3 failed / 59 passed**（cls-arg 端到端仍 `did_expand=True`、新 helper ImportError、corpus 残留集合仍是 cls-arg），实现后 **62 passed**。**反事实实证**（独立脚本，拿 committed HEAD 的 `trace.py` 对照同一份篡改）：cls-arg 假守卫 HEAD `expanded=True safe=True`（完整假阴性：无脑恒真守卫被记成「已扩展且安全」）/ 新尺子 `expanded=False`；`cls-alias` 假守卫两边都 bless（新余量）；「转发给真决策」真守卫两边都 bless（无过收紧）。**数字**：`verifier_strength 0.961538→0.962963`（分母 364→378：26 题型 ×14 → 27×14，被拒 350→364；同口径分解 = 旧 26 题型现在 **364/364 全拒**，新家族 `cls-alias-delegation-hidden` 0/14 仍 bless，**没有为了数字好看删任何家族**）｜`csdd_score=1.0`（14 原则仍全 expanded+safe）｜traceability 14 行不变｜ranking 不变（robust 0.911 / liar 0.400 / reckless 0.311 rejected(probe A1)，帕累托否决未动）｜constitution digest `caae70d1…` 不变（**没改尺子**，Lock-6 完好）。门：**415 passed / 56 skipped**（本 fire +3）、覆盖率 **94.91% ≥ 92**、`ruff check` clean、`ruff format --check` 对本 fire 三个文件 clean（改前该测试文件是被我新行长 assert 弄漂的，HEAD 版本 formatted）。


